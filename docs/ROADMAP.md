# Roadmap — implementation tracker

The authoritative milestone definitions live in
[`../specs/16-milestones.md`](../specs/16-milestones.md). This file
tracks implementation status — what's shipped, what's next. Update on
every iteration.

## Status by milestone

| Milestone | Status | Notes |
|---|---|---|
| **M1** Settings + adapters + AppState | 🟡 in progress — ~97% (Q-A8 resolved 2026-05-07 → M1.h frontend rows unblocked; remaining: M1.h `HeaderBar`/`EmptyProjectState`/`RootPage` build, lifespan-with-real-startup-hook, B-58/B-51 medium bugs — Q-A11/Q-A12 resolved 2026-05-07 per D-040/D-041; B-51/B-58 closeout now blocked-on impl, not user decision) | **iter 48** closed the named M1 acceptance test from `specs/16-milestones.md` line 139 — `tests/integration/test_lifespan.py::test_startup_shutdown_clean — TestClient(build_app(settings)) enters and exits without resource warnings`. New `tests/integration/` directory; pyproject `testpaths=["tests"]` auto-discovers it. Three tests: (1) `test_startup_shutdown_clean` pins no-`ResourceWarning` across the M1.a-through-M1.g wiring graph using `warnings.catch_warnings(record=True)` + `simplefilter("always")` + `gc.collect()` (capture-list path is the only reliable mechanism for finalizer-fired `ResourceWarning`; `simplefilter("error", ResourceWarning)` doesn't propagate from GC-finalizer context per CPython "Exception ignored in:" semantics — verified empirically before settling on the capture-list approach). When M2 layers a real `app_state.startup()` hook on top, this test must keep passing — intent is "no leaks across enter/exit", not "exact resource list". (2) `test_lifespan_runs_when_used_as_context_manager` defends against a `conftest.client` refactor that drops the `with TestClient(app)` form (would silently disable M2+ project discovery without breaking any current test, since M1 lifespan is empty). (3) `test_resource_warning_capture_self_test` is a meta-test that deliberately leaks a file handle to verify the detector finds it — without it, a green `test_startup_shutdown_clean` could mean "no leaks" OR "broken detector"; this distinguishes. Mode `api_only` so SPA-index probe doesn't require populated `static/` bundle in the test sandbox; lifespan invariants are mode-independent. **Also fixed** iter-47's line-length drift in 3 files (`tests/unit/api/test_static_mounts.py`, `tests/unit/core/persistence/{test_paths,test_session_state}.py`) — joined `(arg, arg) -> None` signatures and assertion messages onto single lines under 99 cols. 301 → 304 pytest. ruff lint+format clean. **NOT picked: option A (wire `cli_project_dir`/`source_projects_root` consumers)** — that's a real M2 chunk (project discovery + load + session restore per spec §13), not a "small-M2" — would have shipped a partial half-baked discovery seam; better to ship the full M2 entry deliberately. **M1 progress: ~95%** (only the Q-A8-blocked frontend pieces + the lifespan-with-real-startup-hook remain — both gated). **Open BUGS_FOUND items remaining: B-58 (medium, Q-A12-blocked) + B-51 (medium, Q-A11-blocked).** Iter 49 picks: B-58 once Q-A12 answered, OR M2 entry once Q-A8 unblocked, OR front-run iter-50 review checkpoint. — **iter 47** landed M1.g — `__main__.py` CLI flag wiring per spec `15-deployment-dev.md §3` + `02-backend.md §3`. New flags accepted: `--data-root` (overrides `Settings.data_root`), `--projects-root` (overrides new `Settings.source_projects_root`; consumer lands M2), positional `project_dir` now threads to new `Settings.cli_project_dir` (consumer lands M2), plus legacy parity (`--debugpy`, `-v/--verbose` count, `--page-timing`) accepted at parse time but consumers deferred to later iters (per spec §15 §3 lines 67-77). `Settings` extended with `source_projects_root: Path \| None = None` and `cli_project_dir: Path \| None = None` per spec §3 lines 130-132. New `_build_overrides(args)` helper translates parsed args into the `Settings(**overrides)` dict — only keys the user explicitly passed land in the dict, so argparse `None`/`False` defaults can never clobber `PDLABELER_*` env values (precedence guard pinned by `test_cli_omitted_flag_does_not_override_env` + `test_no_overrides_omits_path_keys_entirely`). 17 new pytest in `tests/unit/test_main_cli.py`: parser-shape (canonical flag set + defaults + `-v` count semantics + `--version` short-circuit), env-vs-CLI precedence (CLI wins where passed; env persists where omitted), `uvicorn.run` kwargs (factory string + `factory=True` + host/port/reload), browser-open semantics (suppressed under `--reload` AND `--no-browser`; opens to `http://<host>:<port>` otherwise), Path-typed flag threading (`--data-root`/`--projects-root`/positional `project_dir`), pyproject `[project.scripts]` entry-point pin, AST guard that the `if __name__ == "__main__":` block calls `main()` and raises `SystemExit`, `main(None)` reads `sys.argv[1:]`. **NOT wired:** `--log-level` — spec §3 only names `log_format` (plain/json), not `log_level`; filed **Q-A13** with recommendation (D) "drop from M1.g — `-v/--verbose` is spec-canonical"; one-line follow-up if user picks (A)/(B)/(C). 284 → 301 pytest. ruff lint+format clean. **M1 progress: ~90%** (M1.a/b/c/d/e/f/g done; remaining: lifespan + FastAPI(lifespan=…) wiring per spec §2 steps 5+6 — deferred until M3 lands JobEventBroker + JobRunner; frontend `HeaderBar`/`EmptyProjectState`/`RootPage` Q-A8-blocked). **Open BUGS_FOUND items remaining: B-58 (medium, Q-A12-blocked) + B-51 (medium, Q-A11-blocked).** Iter 48 picks: B-58 once Q-A12 answered, OR pivot to M2 entry once Q-A8 frontend toolchain unblocked. Iter 50 = next code-review checkpoint. — **iter 46** closed B-57 + B-59 + B-60 + B-61 + B-62 (5 of 6 iter-45 findings; B-58 left — Q-A12-blocked). Five commits, one per bug (`6012f4c`/`e332b14`/`9ced2b7`/`10db73d`/`8657ac8`). 270 → 284 pytest. Highlights: `_serve_image` now catches `OSError` not just `FileNotFoundError` (closes D-003 shared-cache-root surface that compounds B-51); `_resolve_static_dir` uses the `Traversable` abc directly with `resources.as_file()` materialisation for non-`Path` traversables (zip-imported wheels now serve their bundle); `last_project_path_exists()` helper added next to `load_session_state` to spell out spec §6's two-stage validation seam without coupling persistence to FS; `paths.py` purity AST-scan extended to flag `__import__("os")` + `importlib.import_module(...)`; SPA `index.html` served with `Cache-Control: no-store` (hashed assets unchanged) so dev-loop reload no longer risks a stale shell pointing at gone-away asset hashes. Open: B-58 (Q-A12-blocked), B-51 (Q-A11-blocked). M1 sub-tasks remaining: M1.g (`__main__` CLI flag wiring, unblocked) + M1.h (frontend, Q-A8-blocked) + lifespan wiring (deferred to M3). Iter 47 picks M1.g, or resumes B-58 once Q-A12 is answered. — **iter 45** = code-review checkpoint covering iters 41–44 (B-50 ASGI rewrite + B-52/53/54/55 batch + M1.e static_mounts + M1.f persistence). 6 new findings filed B-57..B-62 (0 blocker, 0 high, 2 medium, 2 low, 2 nit). Top finding: **B-58** (medium) — `SessionState.model_config = ConfigDict(extra="forbid")` cites spec §11 (which is about UserPageEnvelope) but the rule under D-003 should match spec §6 + the legacy `from_dict`'s `data.get(...)` extras-tolerance pattern; today's strict shape would silently wipe the user's last session if the legacy ever adds an additive field. Filed **Q-A12** (extras-tolerance policy) in `OPEN_QUESTIONS.md`. Other findings: B-57 (medium, `_serve_image` only catches `ValueError`/`FileNotFoundError`; other `OSError`s leak as 500 — compounds B-51), B-59 (low, `Path(str(traversable))` fails silently on zip-imported wheels), B-60 (low, spec §6 "ignore stale path" half is undocumented caller-responsibility), B-61 (nit, paths.py AST-scan doesn't cover `__import__`/`importlib.import_module`), B-62 (nit, SPA `index.html` served without `Cache-Control: no-store` — dev-loop reload may show stale shell). **B-50 fix verified** by reading both `request_id.py` (raw ASGI middleware with `_lookup_exception_handler` + `send_wrapper`) and the new `test_request_end_emitted_when_call_next_raises` test (`tests/unit/core/test_request_audit_log.py:130-172`). 270/270 pytest still green per iter-44 commit. **Open BUGS_FOUND items: B-51 (medium, Q-A11-blocked) + B-57/B-58 (medium) + B-59/B-60 (low) + B-61/B-62 (nit) = 7.** Iter 46 picks **B-58 first** (medium, one-line fix to `extra="ignore"` + spec §6 amendment + Q-A12 surface), then **B-57** (medium, broaden except clause to `(ValueError, OSError)`), then **B-62** (nit but high-impact UX). Or pivot to **M1.g** (`__main__.py` CLI flag wiring — only unblocked M1 sub-task remaining). M1.h (frontend `HeaderBar`/`EmptyProjectState`/`RootPage`) still Q-A8-blocked. **iter 44 landed M1.f** — `core/persistence/paths.py` + `core/persistence/session_state.py` per spec `01-data-models.md §5` + `09-persistence.md §6`. New pure-derivation module `paths.py` (no I/O, no `os`/`platform` import — OS-awareness lives at one layer in `Settings`, not two): `labeled_projects_root`/`project_backups_root`/`logs_root`/`session_state_path` derive from `data_root`; `image_cache_root` from `cache_root`; `config_yaml_path` from `config_root`; six filename/dirname constants exported (`labeled-projects`/`project-backups`/`logs`/`page-images`/`config.yaml`/`session_state.json` — all pinned literally against the spec). `session_state.py` defines a Pydantic `SessionState` model (`schema_version: str = "1.0"`, `last_project_path: str \| None`, `last_page_index: int \| Field(ge=0)` — legacy field-name spelling for D-003 byte-compat with `pd-ocr-labeler/.../session_state_operations.py`; `extra="forbid"` on the envelope so schema drift surfaces loudly per spec §11) with sync `load_session_state(data_root)` (returns `None` on every failure path: missing file / unparseable JSON / wrong shape / Pydantic validation failure — never crashes startup, debug-logged) and `save_session_state(data_root, state)` (atomic via `tmp + Path.replace`; mkdir's `data_root` on first call so cold installs don't fail; **re-raises** on I/O error rather than silently swallowing — legacy parity break with rationale in module docstring). 30 new pytest in `tests/unit/core/persistence/{test_paths.py,test_session_state.py}` (240 → 270): paths — 6 helper×suffix parametrised cases, purity invariant (no mkdir as side effect), idempotence, distinct-suffix anti-collision, AST-scan forbidding `import os`/`platform` in `paths.py` (catches double-OS-awareness regression); session_state — constants pinned, model defaults + field set + negative-page-index + extra-key rejection, six load failure paths (missing file/missing data root/unparseable/non-object/extra field/negative page_index → all `None`), happy-path round trip, `save_creates_data_root_if_missing`, overwrite semantics, on-disk JSON shape match (key set + types per spec §6, null project path serialises as JSON `null`), legacy-shaped file readability pin (D-003 interop), atomicity: no stray `.tmp` after `Path.replace`, mid-save reader sees complete prior file. ruff lint+format clean; pre-commit clean. **M1 progress: ~85%** (M1.a/b/c/d/e/f done; remaining: lifespan + FastAPI(lifespan=…) wiring per spec §2 steps 5+6 — deferred until M3 lands JobEventBroker + JobRunner; `__main__` CLI flag wiring; frontend `HeaderBar`/`EmptyProjectState`/`RootPage` Q-A8-gated). **Open BUGS_FOUND items remaining: only B-51** (medium, gated on Q-A11). Iter 45 = code-review checkpoint covering iters 41–44 (B-50 ASGI rewrite, B-52/53/54/55 batch, M1.e static mounts, M1.f persistence). — iter 43 landed **M1.e** (`3b55014`): image-cache mount + SPA fallback per spec §2 step 12 + §10 + D-019. New `api/static_mounts.py` with `install_image_cache(app)` (`GET /image-cache/{key:path}` reading bytes via `request.app.state.storage.get_bytes(key)` — IStorage seam per D-019, NOT StaticFiles; path-traversal `ValueError` from FilesystemStorage._path surfaced as 404 so rejection isn't an oracle; `Cache-Control: public, max-age=3600, immutable` since page images are content-addressed; ext-map for image/png\|jpeg\|webp with octet-stream fallback) + `install_spa_fallback(app)` (`GET /{full_path:path}` registered LAST, with reserved-prefix carve-out for `api/*`, `healthz`, `env.js`, `docs/*`, `redoc`, `openapi.json`, `image-cache/*` so unknown `/api/foo` 404s rather than returning HTML; resolves `importlib.resources.files("pd_ocr_labeler_spa") / "static"`, gracefully degrades to a helpful 404 when `index.html` is absent — M0/dev mode — naming `make frontend-build` + the static path, rather than crashing; honours `settings.frontend_dev_url` to flip the catch-all off entirely). `bootstrap.build_app` step 12 now calls all three of `install_env_js` + `install_image_cache` + `install_spa_fallback` inside the `mode != "api_only"` guard, in registration order so the catch-all is last. 219 → 240 pytest (+21 new in `tests/unit/api/test_static_mounts.py`: image serve / 404 / 3 traversal cases / nested keys / Cache-Control / api_only-omits-mounts; SPA index for root + unknown routes / asset passthrough / 3 `/api/*` carve-out cases / 3 real-mount-not-shadowed cases / missing-dist 404 with helpful message / `frontend_dev_url` mode / traversal-into-assets defence / catch-all-is-last invariant). ruff lint+format clean; pre-commit clean. **Open BUGS_FOUND items remaining: only B-51** (medium, gated on Q-A11). Iter 44 picks **M1.f** — `core/persistence/paths.py` + `core/persistence/session_state.py` per spec §13 (OS-aware roots + read/write `session_state.json`); pairs with iter-46's `app_state.startup()` hook. Iter 45 = next code-review checkpoint. — iter 42 closed **B-52 + B-53 + B-54 + B-55 (batched lows/nits — backlog clear modulo Q-A11-gated B-51)**: **B-52 (low)** dropped `# pragma: no cover` from `_state_attr`'s `AttributeError` branch (`api/dependencies.py:38` — line IS exercised by the unwired-FastAPI test); replaced `test_get_storage_raises_runtime_error_on_unwired_app` with `test_provider_raises_runtime_error_on_unwired_app` parametrised over all 5 providers (`get_settings`/`get_app_state`/`get_storage`/`get_auth`/`get_ocr_engine`); tightened `pytest.raises(RuntimeError)` with `match=rf"app\.state\.{missing_attr}.*bootstrap\.build_app"` so the *helpful* parts of the wiring-error message (missing-attr name AND bootstrap pointer) are pinned, not just the exception class. **B-53 (low)** `FilesystemStorage._walk()`'s `is_file()` branch now returns `[base.relative_to(root).as_posix()]` — same canonical root-relative posix form the `rglob` branch produces; previously returned `[prefix.lstrip("/")]` (un-normalised). New `test_filesystem_storage_list_keys_file_prefix_returns_canonical_form` exercises three non-canonical file prefixes (canonical itself, trailing-slash, leading-`./`) and asserts each yields `["page-images/foo.png"]`; pre-fix run produced `['page-images/foo.png/']` for the trailing-slash case (red→green pinned). **B-54 (low)** removed `self._root.mkdir(parents=True, exist_ok=True)` from `FilesystemStorage.__init__`; the factory is now actually pure of FS side effects — `build_app(Settings())` no longer touches the developer's homedir as a side effect of the README smoke test or `make openapi-export`; first write through `put_bytes` still creates the path via the existing `path.parent.mkdir(parents=True, exist_ok=True)` (covers root by transitivity since any key's parent is at least the root). New `test_filesystem_storage_init_does_not_create_root_directory` builds storage at a nonexistent path, asserts the path stays nonexistent post-construction, exercises `put_bytes` to confirm on-demand mkdir works, AND verifies pure-observation paths (`exists`, `list_keys`) on a fresh storage don't create the root either. **B-55 (nit)** removed module-global `_LAST_IDS` from `tests/unit/api/test_dependencies.py`; `_attach_probe_routes` now constructs a fresh `ids` dict per call and the probe-route closures capture it. New `probe_state` fixture yields `(TestClient, ids)`; `test_provider_returns_same_singleton_across_requests` consumes both. `probed_client` retained as a back-compat alias destructuring to just the client. pytest-xdist safe — no cross-test mutable state. 213 → 219 pytest (+6: 5 parametrised B-52 cases — replaced 1 — net +4 + 1 B-53 + 1 B-54 = +6). ruff lint+format clean. **Open BUGS_FOUND items remaining: only B-51 (medium, gated on Q-A11 — user decision on spec-vs-pgdp-parity for 500 body source-line leak).** Iter 43 picks **M1.e** (spec §2 step 11+12 — image-cache mount under `/image-cache` + SPA fallback) since the bug backlog is cleared modulo the gated item; pair with Q-A11 follow-up if user has answered. Iter 45 = next code-review checkpoint. — iter 41 closed **B-50 (high) + B-56 (nit, auto-resolved)**: `RequestIdMiddleware` rewritten from `BaseHTTPMiddleware` to raw ASGI (`__call__(scope, receive, send)`) — rid header now injected on every `http.response.start` via `send_wrapper`, AND `request_end` audit log fires reliably on every path (incl. unhandled-exception) thanks to a real `try/finally` not gated by `BaseHTTPMiddleware` + `collapse_excgroups`. Architectural twist: `Exception`-class handlers are dispatched by Starlette's `ServerErrorMiddleware` which sits OUTSIDE user middleware, so middleware-side header injection is bypassed when the exception escapes; fix delegates by catching the exception in our middleware and dispatching to the `Exception` handler registered on `app.exception_handlers`, sending the response back through `send_wrapper`. Live `TestClient` probe confirms: `RuntimeError` route returns 500 with `X-Request-ID: <rid>` header AND `request_end` audit log fires with `status=500` + non-negative `duration_ms` carrying the correct rid. 3 new tests pin the contracts: `test_handled_error_responses_preserve_request_id_header` extended with `("/_probe/boom", 500)`; new `test_request_end_emitted_when_call_next_raises` and `test_request_id_var_resets_after_exception_request` in `test_request_audit_log.py`. 211 → 213 pytest. ruff lint+format clean. **Remaining open BUGS_FOUND from iter-40:** B-51 (medium, gated on Q-A11), B-52 (low), B-53 (low), B-54 (low), B-55 (nit). Iter 42 picks B-52/53/54/55 batch + Q-A11 follow-up, or pivots to M1.e. Iter 45 = next code-review checkpoint. — iter 40 **code-review checkpoint** (iters 36–39, M1.b/c/d): 7 new findings (B-50..B-56: 0 blocker, **1 high**, 1 medium, 3 low, 2 nit). Top concern: **B-50 (high)** — confirmed by live `TestClient` probe: unhandled-`Exception` 500 responses **drop the X-Request-ID header** AND the `request_end` audit log line is missing on that path; iter-38 author flagged this as a candidate review finding, here it is formalised; root cause is `BaseHTTPMiddleware` + `collapse_excgroups` plumbing — fix requires migrating `RequestIdMiddleware` to a raw ASGI middleware that wraps `send`. Other findings: **B-51 (medium)** — 500 body leaks the source line literally (verified `'boom-secret'` lands in client `details`), contradicts iter-38 commit-message security claim; verbatim port from pgdp-prep but a real disclosure vector once any adapter axis flips multi-tenant. B-52 (low, only `get_storage` failure-path tested), B-53 (low, `list_keys` inconsistent key shape on file-prefix), B-54 (low, `FilesystemStorage.__init__` mkdir side-effect violates "factory is pure"), B-55 (nit, module-global `_LAST_IDS` xdist race), B-56 (nit, `request_end`-on-exception path untested — and B-50 probe shows it's currently broken). 211/211 pytest in 0.50s; ruff + pre-commit clean; `make -n docker-build` returns 0; AppState wire-up smoke OK. Iter 41 picks **B-50 first** (operationally important; auto-resolves B-56) + **B-51** (surface as OPEN_QUESTIONS spec-vs-pgdp-parity decision). Or pivot to M1.e. Iter 45 = next code-review checkpoint. — iter 39: **M1.d** — `core/app_state.AppState` (frozen dataclass: `settings`+`storage`+`auth`+`ocr_engine`) + `build_app_state(settings)` per-axis builder (filesystem rooted at `<cache_root>/page-images`; NoneAuth; LocalDoctrOCR/ModalOCR/SharedContainerOCR all constructible — only `ocr_page` raises `NotImplementedYet` per D-018; `storage_backend="s3"` raises `NotImplementedYet` at wire-time per D-019). New `api/dependencies.py` providers (`get_settings`, `get_app_state`, `get_storage`, `get_auth`, `get_ocr_engine`) read `request.app.state.<name>` directly per spec §6 with a wiring-clear `RuntimeError` fallback when an unwired bare `FastAPI()` hits a provider. `bootstrap.build_app` now calls `build_app_state(settings)` after the settings stash, exposes the singleton on `app.state.app_state` AND each adapter on `app.state.{storage,auth,ocr_engine}` (spec §2 step 9). `Settings` extended with three Literal fields (`storage_backend`, `auth_mode`, `ocr_engine`) sourced verbatim from spec §3. 25 new pytest (`tests/unit/core/test_app_state.py` ×12 + `tests/unit/api/test_dependencies.py` ×13): frozen-shape pin (FrozenInstanceError on assignment + field-set anti-drift), per-axis builder selection, `LocalDoctrOCR`/`ModalOCR`/`SharedContainerOCR` parameterised across `Settings.ocr_engine` literals, s3 wire-time NotImplementedYet, `state.settings is settings` identity, NoneAuth round-trip, TestClient probe routes per provider exercising real FastAPI dependency-resolution, two-request singleton-identity stability, flat-vs-nested storage-id alignment (`app.state.storage is app.state.app_state.storage`), unwired-FastAPI RuntimeError, and direct-call shape (request-like-object, no Depends plumbing). 186 → 211 pytest. ruff lint+format clean. **M1 progress: ~65%** (M1.a/b/c/d done; remaining: full bootstrap wiring per spec §2 steps 5+6 [lifespan + FastAPI(lifespan=…)] — currently steps 1+7+8+9+10+11+12 are wired, steps 2+3+4+5+6 are deferred to when `JobEventBroker` + `JobRunner` land in M3 since spec §2 step 4 wants those in `AppState` — `core/persistence/{paths,session_state}`, `__main__` CLI flag wiring, frontend `HeaderBar`/`EmptyProjectState`/`RootPage` [Q-A8-gated]). Iter 40 = code-review checkpoint covering iters 36–39 (M1.b/c/d). Reviewer should focus on: (1) handler-4 `X-Request-ID` echo gap from iter 38 — Starlette ServerErrorMiddleware ordering, candidate for ASGI-shape `RequestIdMiddleware` rewrite; (2) `AppState` dataclass field set vs spec §2 step 4 signature `AppState(settings, storage, auth, ocr_engine, broker, runner)` — is deferring `broker`/`runner` to M3 the right call, or should they be `None`-typed seams now? (3) flat `app.state.storage` shadow-stash duplication vs nested `app.state.app_state.storage` — single-source vs explicit pair? (4) provider-failure-path RuntimeError message quality vs the spec §6 contract; (5) Settings extension non-breakingness (verified by existing `test_settings.py` 8 tests). Prior iter 38 (`0663818`): **M1.c** — `error_handler` middleware (spec §8) landed. New `core/exceptions.BoundingBoxGeometryError(ValueError)` + `api/middleware/error_handler.py` (`ApiError` envelope `{error, message, details}` + `install_error_handlers(app)` registering 4 `@app.exception_handler` decorators: `StarletteHTTPException` → `http_<n>` preserving status; `RequestValidationError` → 400 `validation_error` + `details=exc.errors()`; `BoundingBoxGeometryError` → 422 `geometry_error` (labeler-specific addition vs pgdp-prep verbatim port); `Exception` catch-all → 500 `internal_error` with last-3 traceback lines + `logger.exception` for full server-side stack). Wired into `bootstrap.build_app` after `app.state.settings` stash and before `install_healthz`, so error responses still route through CORS + RequestIdMiddleware on the way out. 12 new tests (`tests/unit/core/test_error_handler.py`) pin one branch per handler + envelope shape, snake_case ascii tag character class, `build_app` wiring (all 4 classes registered on `app.exception_handlers`), idempotency under double `install_error_handlers`, ERROR-level traceback emission for handler 4, and request-id header echo on handlers 1+3. **Architectural gap discovered & documented (handler 4 only):** Starlette's `ServerErrorMiddleware` sits ABOVE user middleware in the framework's ASGI stack — an unhandled `Exception` bubbles past `RequestIdMiddleware.dispatch`'s `call_next` BEFORE our 500 response is built, so the X-Request-ID header isn't echoed on catch-all 500s. Handlers 1/2/3 are unaffected (FastAPI's `ExceptionMiddleware` runs them inside the user-middleware stack). Shared with pgdp-prep, out of scope for M1.c, candidate for next code-review checkpoint (possible fix: rewrite `RequestIdMiddleware` as pure-ASGI shape that wraps `send` rather than awaiting `call_next`). 174 → 186 pytest. ruff lint+format clean. **M1 progress: ~50%** (M1.a + M1.b + M1.c done; remaining: `core/app_state` skeleton, `api/dependencies`, full `bootstrap` wiring per spec §2 steps 2-6+9-12, `core/persistence/{paths,session_state}`, `__main__` CLI flags, frontend `HeaderBar`/`EmptyProjectState`/`RootPage` [Q-A8-gated]). Iter 39 picks **M1.d — `core/app_state` skeleton + `api/dependencies` module** (spec §6, §2 step 4) for continued M1 code-progress; iter 40 = next code-review checkpoint. iter 37 closed B-44 + B-45 + B-46 + B-48 + B-49 (batched 5-bug closeout): **B-44** (medium) — `FilesystemStorage.{put_bytes,delete,list_keys}` now dispatch their sync FS calls (`mkdir(parents=True)`, `exists()`/`unlink()`, `rglob` walk) through `anyio.to_thread.run_sync(...)` so async methods no longer block the event loop. Pinned by new AST-scan test that walks `filesystem.py` and flags any bare sync `Path.{exists,unlink,mkdir,rglob,is_file,relative_to}()` call lexically inside `async def` (correctly excluding `await` expressions and nested `def`/`lambda` bodies passed to the threadpool). **B-45** (low) — path-traversal guard now raises `ValueError` up-front when `key.startswith("/")` (was silently re-rooting under `self._root`); docstring rewritten; new `test_filesystem_storage_absolute_key_rejected` exercises every `IStorage` async method on `/etc/passwd` and asserts no re-rooted side-effect dir appears. **B-46** (low) — dropped `(IOCREngine)` base from `LocalDoctrOCR`/`ModalOCR`/`SharedContainerOCR` (structural-only conformance policy now matches storage/auth + pgdp-prep); `adapters/__init__.py` grew a "Conformance policy" section spelling out the rule; new `test_ocr_impls_conform_structurally_not_by_inheritance` pins `IOCREngine not in __mro__` AND `isinstance(impl(), IOCREngine)` still passes via `@runtime_checkable`. **B-48** (nit) — caplog filter cleanup is now identity-based (`removeFilter(rid_filter)` instead of `filters[-1]`). **B-49** (nit) — `test_request_id_var_default_is_empty_string` now uses a fresh `contextvars.Context()` to read the *declared* default rather than circularly setting then asserting `""`. 171 → 174 pytest (+3 net new shape pins). ruff lint+format clean. **Open BUGS_FOUND items: 0** (iter-35 review backlog cleared going into iter 38). Iter 38 picks M1.c — `error_handler` middleware (spec §8) — for code-progress; iter 40 = next code-review checkpoint. iter 36 closed B-42 + B-43 + B-47: **B-43** — `RequestIdMiddleware.dispatch` now emits the spec §9 per-route audit log (`request_start` info on entry with `path`+`method`; `request_end` info on exit with `path`+`method`+`status`+`duration_ms`). Both lines emitted inside the request-id ContextVar scope so `RequestIdFilter` tags them with `rid=`. `time.monotonic()` for duration measurement; `status_code` defaults to 500 if `call_next` raises. **B-42** — `IAuth.verify` reverted to spec signature `creds: HTTPAuthorizationCredentials \| None` (was `credentials: str \| None`);`NoneAuth` mirrors. New `test_iauth_verify_signature_matches_spec` uses `typing.get_type_hints` to resolve PEP-563 string annotations and pin parameter name + type + return — future drift fails loudly. **B-47** — autouse `*reset_managed_handlers` fixture lifted into new `tests/unit/core/conftest.py` so it applies session-wide; duplicate dropped from `test_logging_config.py`. Test count 167 → 171 (+4: 3 audit-log + 1 signature drift-pin). ruff lint+format clean. Open BUGS_FOUND items: B-44, B-45, B-46, B-48, B-49 (5 remaining: 1 medium, 2 low, 2 nit). Iter 37 should pick **B-44** (medium — sync FS calls under`async def`; ~6-line fix wrapping with`anyio.to_thread.run_sync`). iter 33 (`2bb956d`): adapter Protocols + impls landed (`adapters/{storage,auth,ocr}/{base,…}` + `core/exceptions.py::NotImplementedYet`).`IStorage` Protocol fixed by `specs/02-backend.md §7`;`FilesystemStorage` round-trip + path-traversal guard tested (rejects `..`-escape and absolute-path keys).`IAuth` + `UserContext(user_id, display_name)` + `NoneAuth.verify` returning `("local", "Local User")` for any input (D-005). `IOCREngine` + `LocalDoctrOCR` (raises bare `NotImplementedError` until M3 wires it) + `ModalOCR`/`SharedContainerOCR` (raise spec-named `NotImplementedYet` per D-018). `core/exceptions.NotImplementedYet` subclasses `NotImplementedError`. 15 new pytest tests across`test_adapters*{storage,auth,ocr}.py`(136 → 151). No frontend dependency, no Q-A8 blocker. iter 34 (`b70ec57`): **M1.b** —`RequestIdMiddleware`(`api/middleware/request_id.py`) + structured logging (`core/logging_config.py`) as verbatim ports from pgdp-prep per spec §9.`request_id_var: ContextVar[str]` (empty-string default) + `RequestIdFilter` injects `record.request_id` + `JsonFormatter` (stable `{ts, level, logger, msg, request_id}` schema, extras folded, `exc_info`→string`exc`). Plain formatter line shape`[rid=%(request_id)s]` so `grep 'rid='` works without parsing JSON. `configure_logging(log_format)` is idempotent — managed handler tagged `_pdlabeler_managed=True` so `--reload` doesn't stack handlers AND caplog/uvicorn handlers aren't accidentally evicted. Bootstrap now calls `configure_logging` first (spec §2 step 1) and adds `RequestIdMiddleware` LAST so Starlette's `user_middleware` places it OUTERMOST (spec §12: index 0 = RequestId; CORS inside it). 16 new pytest tests across `tests/unit/core/test_request_id.py` (7) and `tests/unit/core/test_logging_config.py` (9) — including the spec-named acceptance assertions `test_request_id_echoed`,`test_request_id_generated_when_absent`, plain-formatter contains`rid=abc`. 151 → 167. Remaining M1 sub-tasks:`error_handler` middleware, `core/app_state` skeleton, `api/dependencies`, full`bootstrap.build_app` wiring (steps 2-6 + 9-12 per spec §2), `core/persistence/{paths,session_state}`,`**main**` CLI flag wiring, frontend `HeaderBar`/`EmptyProjectState`/`RootPage`(Q-A8-gated). iter 35 **code-review checkpoint** (iters 31-34) → 8 new findings (B-42..B-49: 0 blocker, 0 high, 3 medium, 3 low, 2 nit). Top concerns: **B-43 (medium)** — spec §9's audit-log enhancement (`request_start`/`request_end` info logs naming path/method/status/duration_ms; explicitly "closes pgdp-prep gap" per spec) was *quietly skipped* on iter-34's "verbatim port from pgdp-prep per spec §9" commit, not in remaining-M1-sub-tasks list, gap is invisible. **B-42 (medium)** — `IAuth.verify` parameter type drifted from spec's `creds: HTTPAuthorizationCredentials \| None` to impl's `credentials: str \| None` without a spec-edit-first decision. **B-44 (medium)** — `FilesystemStorage.delete`/`list_keys`/`put_bytes` parent-mkdir use sync FS calls under `async def` (defeats async; `put_bytes` comment admits the shortcut). Plus B-45 (low, path-traversal docstring vs impl mismatch on absolute keys), B-46 (low, OCR impls subclass Protocol while auth/storage don't), B-47 (low, missing autouse cleanup in `test_request_id.py`), B-48/B-49 (nits). **M1 progress: ~35%** — M1.a + M1.b done; ~6 chunks remain. Iter 36 should pick **B-43 first** (spec gap, ~10 lines), then **B-42** (signature drift, decision required) — pairs with B-47. Alternatively pivot to M1.c (error_handler middleware, spec §8) for code-progress over backlog cleanup. Iter 40 = next code-review checkpoint. |
| **M0** Repo scaffold | 🟡 in progress (Q-A8 resolved 2026-05-07 per D-036; Q-A9 resolved 2026-05-07 per D-037 — pending the implementation iteration that lands the flat ESLint config + restores the `lint` script) | Iter 1 backend skeleton + tests; iter 2 frontend scaffold (files only); iter 3 `mise.toml` + Makefile + Makefile parse smoke tests; iter 4 `.pre-commit-config.yaml` mirroring pd-prep-for-pgdp + YAML-shape smoke tests; iter 5 **code-review checkpoint** → 9 findings filed in `BUGS_FOUND.md`; iter 6 fixed B-02 + B-03; iter 7 fixed B-01 + B-09; iter 8 fixed B-05 + B-06 + B-08; iter 9 fixed B-04 (Settings now `frozen=True` + `Settings(**overrides)` in `__main__`) + B-07 (`_build_env()` no-arg) and added `docs/DEVELOPMENT.md` with shape-pin tests; iter 10 **code-review checkpoint** → 5 new findings (B-11..B-15: 2 low, 3 nit; no blockers); iter 11 fixed B-12 + B-13 + B-14 + B-15; iter 12 fixed B-11 (post-commit pre-commit hook auto-runs `make refresh-version` so `__version__` stays current); iter 13 wired Tailwind v3.4 (`tailwind.config.js`, `postcss.config.js`, `src/index.css` with three `@tailwind` directives, `main.tsx` imports it, devDependencies pin v3.x/v8.x/v10.x); iter 14 added `Dockerfile` (three named stages: `spa`/`wheel`/`runtime`, Node 24 + Python 3.13 matching `mise.toml`, `COPY --from=spa` lands SPA at `src/pd_ocr_labeler_spa/static/` for `build_hooks/spa_check.py`, runtime `EXPOSE 8080` + `pd-ocr-labeler-ui --host 0.0.0.0 --no-browser`) + `.dockerignore`; iter 15 **code-review checkpoint** → 7 new findings (B-16..B-22: 1 high, 2 medium, 2 low, 2 nit). Top concern: B-16 (Dockerfile `ENV PD_LABELER_HOST/PORT` doesn't match `Settings` `PDLABELER_*` prefix — env-name underscore mismatch). iter 16 fixed B-16 (dropped dead `ENV PD_LABELER_*` lines from `Dockerfile`; new test sources the prefix from `Settings.model_config["env_prefix"]` at runtime so any future drift fails loudly) + B-17 (added `post-rewrite` + `post-checkout` pre-commit hook stages, all converging on new `scripts/refresh_version_git_hook.sh` so amend/rebase/cherry-pick no longer leave `__version__` stale) + B-22 (paired doc-honesty annotation on B-11). iter 17 fixed B-20 (Dockerfile runtime stage now installs from a frozen `uv export` requirements.txt with `--no-deps` wheel install, so transitive deps come from `uv.lock` instead of fresh PyPI resolution) + B-21 (runtime apt-get install/purge git+ca-certificates inside a single RUN so the final image carries no git binary) and corrected the iter-12 backlog-zero annotation to reference subsequent iter-15 findings. iter 18 added `make docker-build` / `docker-run` / `docker-shell` targets (mirrors pd-prep-for-pgdp shape; `docker-build` depends on `frontend-build`; `docker-run` maps `$(DOCKER_PORT):8080`) plus `tests/unit/test_makefile_docker.py` (10 tests: dry-run parse for each target, help-output coverage, default tag pin, three-way Settings/Dockerfile-EXPOSE/Makefile-`-p` port alignment, .PHONY coverage). iter 19 added `install.sh` (POSIX bash, mirrors pd-prep-for-pgdp installer pattern: `uv tool install` against the wheel attached to the latest GitHub Release; no Node/npm required at install time; ~75 lines) plus `tests/unit/test_install_sh.py` (10 tests: existence + executable bit, bash shebang + `set -euo pipefail`, Python pin sourced from `mise.toml`, entrypoint name sourced from `pyproject.toml [project.scripts]`, repo slug sourced from `[project.urls].Homepage`, `bash -n` syntax check, `uv tool install` peer-mirror pin, ~80-line size budget). iter 20 **code-review checkpoint** → 5 new findings (B-23..B-27: 0 blocker, 0 high, 1 medium, 2 low, 2 nit). Top concern: B-23 (no `uv lock --check` gate anywhere — B-20 made docker-build the only place lockfile drift surfaces). iter 21 fixed B-23 (added `uv-lock-check` pre-commit hook in the `local` repo block running `uv lock --check` with `language: system` + `files: ^(pyproject\.toml\|uv\.lock)$`; new `tests/unit/test_uv_lock_check.py` with 2 tests pinning hook shape + asserting current lockfile passes `uv lock --check` as a subprocess) + B-26 (flipped M0 sub-task checkboxes for iter-18 `docker-*` and iter-19 `install.sh`, split out a new `install.ps1` pending bullet). iter 22 fixed B-24 (added `_docker` Makefile macro analogous to `_npm` so docker-* targets emit a friendly diagnostic with install pointers when docker isn't on PATH; all three recipes dispatch via `$(call _docker,…)`) + B-25 (added a real`python3 -c "import sys; ..."` preflight check to `install.sh` so the script behaviourally inspects the system Python — was previously comment-only; new `test_install_sh_runs_python_version_preflight` pins the behavioural invariant) + B-27 (switched `install.sh` from `/repos/X/tags` to `/repos/X/releases/latest` — single call, robust to pre-1.0 retag history; new `test_install_sh_uses_releases_latest_endpoint` pins both the new endpoint and forbids the bare `/tags` regression). Frontend `npm install` still blocked on Q-A8. `install.ps1` (Windows) and `release.yml`(GitHub Actions) still pending for M0 acceptance gate; shadcn primitives still gated on Q-A8. iter 23 (`d2bab21`): added`install.ps1`(PS 5.1+ compatible mirror of install.sh) + 9 shape pins. iter 24 (`8a848c8`): added`.github/workflows/release.yml` (publish wheel + sdist on `v*` tag push, 12 shape pins; PyPI deferred per Q-A10). iter 25 **code-review checkpoint** → 9 new findings (B-28..B-36: 0 blocker, 1 high, 1 medium, 2 low, 5 nit). Top concern: **B-28** — `release.yml` runs `npm ci` against an absent `frontend/package-lock.json` (M0-load-bearing; coupled with B-19); release pipeline can't run end-to-end on first tag push. Second: **B-29** — `tags: ["v*"]` is too permissive (matches `vfeature-test` etc.). **M0 acceptance gate not yet declarable complete:** clauses still blocked on Q-A8 (frontend toolchain — no npm/Node available, blocks `make frontend-build`/`frontend-install`/`frontend-test` runtime verification AND the package-lock.json generation that B-28 needs), Q-A9 (eslint config + `lint` script restoration), and B-28 (release pipeline). Iter 26 picks B-28 first; an M0-close iteration needs Node availability resolved first. iter 26 fixed **B-28 + B-19 (paired)**: both `release.yml` "Build SPA bundle" step and `Dockerfile` `spa` stage rewritten to a two-pass install — `if [ ! -f package-lock.json ]; then npm install --package-lock-only; fi && npm ci` — so the first tag push and the docker build both succeed regardless of whether a real `frontend/package-lock.json` is committed (Q-A8 still blocking that). Once a real lockfile lands, the bootstrap branch is a no-op and `npm ci` stays the source of truth. Three new regression tests: `test_uses_two_pass_install_with_lockfile_fallback` (release workflow), `test_spa_stage_uses_npm_ci_with_lockfile_fallback` (Dockerfile), `test_dockerfile_and_release_workflow_agree_on_npm_install_logic` (cross-file alignment guard so future tightenings can't drift the two stages back into the iter-25 inconsistency). Existing `test_uses_npm_ci_not_npm_install` loosened to allow the bootstrap form while still forbidding bare `npm install`. M0 release-pipeline blocker removed; remaining M0 blockers are Q-A8 (Node unavailability) + Q-A9 (eslint config). iter 27 (`c6eabad`): fixed B-29 (PEP-440 tag globs) + B-30 (concurrency block) + B-31 (npm + uv caching) + B-35 (tightened`uv tool install` test) + B-36 (release.yml comment re-word). iter 28: fixed B-32 + B-33 + B-34. **B-32:** `Test-Command` rewritten as `return $null -ne (Get-Command -Name $Name -ErrorAction SilentlyContinue)` — explicit Boolean, no array-on-success-path coercion. **B-33:** Python preflight now invokes `& python --version 2>&1` and matches `^Python \d+\.\d+\.\d+$` via `-notmatch` so the Microsoft Store stub redirector is detected and surfaces a clear "install real Python from python.org / `winget install Python.Python.3.13`" message. **B-34:** dropped `python-version` from `astral-sh/setup-uv@v4`'s `with:` block — `uv build` provisions its own PEP 517 build-isolated Python, so the setup-uv pin was redundant (~5s wasted CI per run). Renamed `test_python_version_matches_mise` → `test_python_pin_in_release_workflow` and loosened to accept comment-mention so the `mise.toml` Python pin still drift-checks against this workflow. New tests: `test_test_command_returns_explicit_boolean`, `test_install_ps1_detects_ms_store_stub_python`, `test_setup_uv_does_not_set_python_version`. install.ps1 size budget bumped 120→140 lines (B-33 stub-detection branch). 130/130 pytest passing. Open BUGS_FOUND items remaining: only **B-18** (Tailwind glob fragility, low). iter 29: closed **B-18** (rewrote the Tailwind shape-pin test to parse the `content: [...]` array and assert it CONTAINS the canonical `./src/**` scan, tolerant to additive shadcn/ui evolution; brace-expansion entries with extra extensions accepted as long as `ts`/`tsx` are in the set) and added **`docs/M0-acceptance.md`** (single-page sign-off doc — eight criterion clauses straight from `specs/16-milestones.md` §M0, current status row-by-row, Q-A8/Q-A9 remaining-blockers section, and the six-step ritual that flips M0 from "in progress" to "done"). New `tests/unit/test_m0_acceptance.py` (6 shape pins: file exists, `## Status` section, Q-A8/Q-A9 named, every spec-mandated `make` token referenced, sign-off ritual section, spec+ROADMAP citations). Test count: 130 → 136. **All open BUGS_FOUND items closed going into iter 30.** iter 30 **code-review checkpoint** (iters 26–29) → 5 new findings (B-37..B-41: 0 blocker, **1 high**, 0 medium, 2 low, 2 nit). Top concern: **B-37 (high)** — `actions/setup-node@v4` `cache: "npm"` + missing `frontend/package-lock.json` will hard-fail the workflow at the Setup Node.js step BEFORE the iter-26 two-pass install gets a chance to bootstrap the lockfile, silently re-opening B-28. Other findings: B-38 (low, `--include=dev` asymmetry between Dockerfile and release.yml — "byte-aligned" framing overstates parity), B-39 (nit, `test_python_pin_in_release_workflow` accepts any `3.13` substring after iter-28 loosening — pinned to a comment, not a real workflow pin), B-40 (nit, install.ps1 MS Store stub regex `^Python \d+\.\d+\.\d+$` rejects pre-release Python like `3.14.0a1` and mislabels it as a stub), B-41 (low, cross-file `--package-lock-only` pin is planned-obsolete; future Q-A8-unblock iter must remove from BOTH files AND the test, no in-test breadcrumb). 136/136 pytest still green; ruff + pre-commit clean. **M0 status (re-assessed):** still NOT declarable complete — three blockers: Q-A8 (frontend toolchain, gates 4/8 acceptance criteria), Q-A9 (ESLint config, gates 1/8), and B-37 (release pipeline cannot run end-to-end due to setup-node hard-failing on missing lockfile). Iter 31 should pick **B-37 first** (one-line fix: drop `cache: "npm"`until Q-A8 lands the lockfile), bundled with B-38 (`--include=dev` symmetry). iter 31 fixed **B-37 + B-38 (paired)**: dropped `cache: "npm"` + `cache-dependency-path` from `actions/setup-node@v4` (with inline comment + Q-A8 breadcrumb), and added `--include=dev` to BOTH npm invocations in `release.yml`'s Build SPA bundle step so Dockerfile/workflow are flag-set-symmetric. Replaced`test_setup_node_enables_npm_cache` with `test_setup_node_npm_cache_disabled_until_lockfile_lands` (YAML-walk forbidding both `cache:` and `cache-dependency-path:` keys until Q-A8 lands); tightened `test_dockerfile_and_release_workflow_agree_on_npm_install_logic` to assert `--include=dev` symmetry on every `--package-lock-only` and `npm ci` line in both files (non-comment only, so a future comment-only mention can't satisfy the symmetry alone). 136/136 pytest still green; ruff lint+format clean. **M0 release pipeline blocker B-37 closed**; remaining M0 blockers are Q-A8 (frontend toolchain, gates 4/8 acceptance criteria) + Q-A9 (ESLint config, gates 1/8). Open BUGS_FOUND items: **B-39** (nit, `test_python_pin_in_release_workflow` `3.13` substring is now coupled to a comment) + **B-40** (nit, install.ps1 MS Store stub regex rejects pre-release Python like `3.14.0a1`) + **B-41** (low, cross-file`--package-lock-only` pin is planned-obsolete; future Q-A8-unblock iter must remove from BOTH files AND the test). Iter 32 will batch B-39 + B-40 + B-41. iter 32 fixed **B-39 + B-40 + B-41 (batched)**: **B-39:** test renamed `test_python_pin_in_release_workflow` → `test_python_pin_in_release_workflow_matches_mise_if_set` and reshaped from prose-coupling ("must mention `3.13` somewhere") to a meaningful drift check (any step's `with: { python-version: ... }` value must equal `_mise_pin("python")`; today no key exists, so the assertion is a no-op until a future setup-python step re-introduces a pin). **B-40:** install.ps1 MS Store stub regex loosened from`^Python \d+\.\d+\.\d+$` to `^Python \d+\.\d+(\.\d+)?` (anchor on major.minor, optional patch, allow any trailing characters), so `Python 3.14.0a1`,`Python 3.14.0rc2`, and`Python 3.13.0+` (pyenv-built) all match while the stub's "Python was not found" reparse-output is still detected; diagnostic message reworded to lead with what was checked rather than asserting "this is the Store stub" up front. **B-41:** added explicit PLANNED-OBSOLESCENCE breadcrumb docstrings to BOTH `test_uses_two_pass_install_with_lockfile_fallback` (release workflow) and `test_dockerfile_and_release_workflow_agree_on_npm_install_logic` (cross-file Dockerfile/release alignment), naming Q-A8 as the unblock trigger and enumerating the four-place cleanup that must land in a single commit (drop bootstrap from release.yml + Dockerfile, drop the workflow-side assertion, drop the cross-file `--package-lock-only` clauses). 136/136 pytest still green; ruff lint+format clean; install.ps1 at 139 lines (under 140 budget). **Open BUGS_FOUND items: 0** (entire iter-30 review backlog closed). Iter 33 should resume scaffolding — natural next chunks are M1 entry specs, shadcn-init prep that doesn't require Node, or a `CHANGELOG.md`. Iter 35 = next code-review checkpoint. |
| M2 Project discovery + load | 🟡 in progress — slices 1+2+3+4+5 shipped (~70%); persistence I/O wired, ProjectState carries loaded Project | **Iter 5 (loop)** landed M2 slice 5 — persistence I/O. New `core/persistence/ground_truth.py` (byte-compat re-implementation of legacy `load_ground_truth_from_directory`: `pages_manifest.json` wins over `pages.json`, manifest-source-merge in declaration order with later-wins-on-collision, integer offset applied to numeric stems via `%03d` zero-pad, PGDP normalization via `PGDPResults.processed_page_text`, lowercase + extension-less aliases via `setdefault`, every failure mode → warn + `{}`). New `core/persistence/project_envelope.py` with `project_to_dict`/`project_from_dict`/`read_project_metadata`/`build_project_from_directory` — `to_dict`/`from_dict` byte-compat with legacy `Project.to_dict`/`from_dict` (`project_model.py:42-71`); `copied_images` D-003 friction handled at the boundary (legacy int↔SPA bool: int 0 → False, non-zero → True; SPA writes bool which legacy reads as int(1)/int(0)); `read_project_metadata` returns `None` on every failure mode mirroring legacy "don't break existing projects"; `build_project_from_directory` composes image-scan + metadata-read + caller-supplied GT with on-disk overrides winning (project_id from basename, total_pages from disk count), `current_page_index` clamped to `[0, total_pages-1]` so a stale-cursor save can't crash `ProjectState.set_current_page_index`'s upper-bound check. Route upgrade: `POST /api/projects/load` now calls `load_ground_truth_from_directory` + `build_project_from_directory`, swaps both the `ActiveProjectCarrier` (path) and the new `ProjectState` (graph — wired into `bootstrap.build_app` + `api/dependencies.get_project_state`), and returns the spec-canonical `LoadProjectResponse{project, current_page_index, generation}` (replacing slice-4 `LoadProjectResponseStub`). Spec-canonical `current_page: PagePayload` deferred to M3 (PagePayload requires PageRecord/EncodedDims/LineMatch[]/image-cache-URLs); slice 5 ships `current_page_index: int` instead — URL stays stable so M3 expansion is field-rename only. 41 new tests across `tests/unit/core/persistence/test_ground_truth.py` (18: missing/empty/non-dict-root/invalid-JSON, single-file, lowercase + extension aliases, manifest-single-source/offset-remap/two-source-merge/disjoint-keys, manifest-fallback, missing-source-skipped, invalid-entry-skipped, non-dict-manifest-root-fallback, PGDP normalizer call), `tests/unit/core/persistence/test_project_envelope.py` (19: filename pin, to_dict key-set + source_path-string + copied_images-bool, from_dict legacy-int-zero/legacy-int-nonzero/preserves-metadata/legacy-defaults-for-missing-keys, to_dict↔from_dict round-trip identity, read_project_metadata missing/success/invalid-JSON, build scan-sorted/basename-id/resolved-root/GT-passthrough/uses-project_json/no-project_json-defaults/total_pages-from-disk/clamped-cursor/zero-images-empty), and `tests/integration/test_projects_router.py` upgraded (slice-4 stub assertions replaced; +2 new integration tests: real-images+GT via `pages.json` fixture, `ProjectState` mutation pin via direct `app.state.project_state` read). 422 → 463 pytest. ruff lint+format clean. **Next iter** options: M2-proper `GET /api/projects/{id}` + session-state writeback (small slice; session writer needs the page index now in the response), or pivot to M3 first-page OCR (heavier; needs OCRConfigModal + image cache + `ensure_page_model`). — **Iter 4 (loop)** landed M2-proper `core/project_state.py` skeleton + minimal `core/models.py::Project` (the spec §1 lines 28-44 model, `extra="forbid"` envelope, `page_count` property). New `ProjectState` carrier holds `loaded_project: Project \| None`, `page_states: dict[int, PageState]` (placeholder dataclass — rich M3 shape with `pd_book_tools.ocr.page.Page` + dirty flags + selection sets is M3), `current_page_index`, monotonically-increasing `generation` counter for SSE/cache-invalidation, `threading.Lock` for sync+async safety. Mutations: `set_loaded_project(project)` (resets per-page-state map — page indices are scoped to ONE project — and seeds `current_page_index` from `Project.current_page_index` so resume-from-disk parity matches legacy), `clear()`, `get_page_state(idx)`, `set_page_state(idx, state)` (validates `page_index >= 0` + `state.page_index == key`), `set_current_page_index(idx)` (validates non-negative + < `total_pages` when project loaded — under the lock so concurrent `set_loaded_project`/`clear` can't race the upper-bound check). Renamed misnamed iter-3 `tests/unit/core/test_project_state.py` → `test_active_project.py` to free the spec-canonical slot (the file actually tested `core.active_project`); new 17 unit tests in `tests/unit/core/test_project_state.py`: Project shape (defaults match spec §1, `page_count` property), empty-state defaults, set_loaded_project (stores+bump, resets page-state map across project swaps, seeds current_page_index), clear (full reset + generation bump), get/set page_state (None-on-miss, round-trip, generation bump, negative-index reject, key-vs-state mismatch reject), set_current_page_index (in-bounds, negative reject, out-of-range when loaded), 16-thread concurrent set_page_state (every successful mutation == one bump → generation == N), separation-of-concerns pin (no filesystem validation — that lives on `ActiveProjectCarrier` for project root + slice-5 persistence for project contents). 398 → 415 pytest. ruff lint+format clean. **Slice 5 (next iter)**: persistence I/O — `core/persistence/project_envelope.py` reading `pages.json` / `pages_manifest.json` + GT scan + `Project.from_dict`/`to_dict` (legacy v2.1 byte-compat); then wire `POST /api/projects/load` to call slice 5 → `state.set_loaded_project(project)` → extend `LoadProjectResponseStub` to the spec-canonical `LoadProjectResponse{project, current_page}`. `LoadProjectResponseStub` left in place this iter — the route handler can't return a real `Project` until persistence lands. — **Iter 3 (loop)** landed slice 4 second half — `api/projects.py` router with `GET /api/projects` (composes the iter-2 `enumerate_projects` scanner + `Settings` + `ActiveProjectCarrier` → `ListProjectsResponse{projects, selected, projects_root, config_source}`; `selected` is omitted when the active project lives outside `source_projects_root` — the dropdown can only mark entries it actually shows; `config_source: "default"` unconditionally for slice 4, M2-proper's `POST /api/projects/source-root` will start tracking `"yaml"`/`"cli"`) and `POST /api/projects/load` (validates path-resolves-under-root via `Path.resolve()` symmetric on both sides so `..` traversal is rejected; emits `404 project_not_found`/`400 invalid_project_dir`/`400 project_outside_source_root`/`400 validation_error` per spec §8 envelope shape — flat `{error, message, details}`, NOT raised via `HTTPException` because the framework's StarletteHTTPException handler would relabel the tag to `http_<n>`). Returns interim slim `LoadProjectResponseStub{project_key, generation}` because the spec-canonical `LoadProjectResponse{project: Project, current_page: PagePayload}` requires the M2-proper `Project` graph + `PagePayload` which don't exist yet — Stub-suffixed name keeps generated TS types stable for the M2-proper purely-additive expansion. Session-state writeback intentionally omitted at slice 4 (writer needs page-payload plumbing to know the page index); bundled with the M2-proper page-payload extension. 18 new integration tests in `tests/integration/test_projects_router.py` (380→398): GET — sort case-fold/skip-hidden+files/resolved-path/label-default/empty-carrier/projects-root-echo/config-source-default/empty-list-no-root/select-after-load/select-omitted-if-off-root; POST — happy-path/idempotent-generation-bump/missing-dir-404/regular-file-400/outside-root-400/no-root-configured-400/dotdot-traversal-rejected/missing-body-validation. Bootstrap registers via `install_projects_router(app)` after `install_healthz`, before SPA fallback. ruff lint+format clean. **Iter 2 (loop)** landed slice 4 starter — `core/project_enumeration.py` pure scanner (no I/O mutation; case-fold primary sort + raw-name secondary tiebreak; filters: hidden dirs, regular files, broken symlinks; dedup-on-label rule per spec §2 line 215 — collisions get suffixed `(2)`/`(3)` labels but `project_id` itself stays unique-per-basename so URLs don't shift) plus 13 unit tests in `tests/unit/core/test_project_enumeration.py`. Iter 2 also closed **B-58** + landed **D-041** — `SessionState.model_config = ConfigDict(extra="ignore")` with WARNING-level drift signal `session_state_extras_dropped` (stable substring grep-able from both SPA and legacy logs); two old `extra="forbid"` tests rewritten, two new D-041 contract tests added. **Iter 55** landed slice 3 — FastAPI lifespan startup hook in `bootstrap.py` (`_make_lifespan(settings, carrier)` factory closure) reads `load_session_state(settings.data_root)`, calls `resolve_initial_project(settings, session_state=...)`, and feeds the resolved `path` into `carrier.set_active_project(path)` if not None. Carrier+lifespan are now constructed BEFORE `FastAPI(...)` so the closure can capture the same carrier instance that's later stashed on `app.state.active_project_carrier`; the dual-stash is intentional and pinned by `test_lifespan_startup_hook_is_idempotent_across_app_builds` (per-app singleton, not module-global). TOCTOU between resolver-validate and carrier-validate is handled: an `InvalidProjectDirError` from `set_active_project` is logged WARNING and the carrier is left empty — boot doesn't refuse over a vanishing dir. 7 new integration tests in `tests/integration/test_startup_discovery_wiring.py` (358→365): happy-path (CLI dir → carrier), no-inputs (carrier stays None), session-restore (no CLI + valid session_state.json), stale-session (graceful None), CLI-overrides-session (precedence), invalid-CLI-falls-through-to-session, idempotency-across-app-builds. ruff lint+format clean. **Slice 3 deliberately does NOT**: enumerate `source_projects_root` (M2-proper), add `POST /api/projects/load` (slice 4), persist a new `session_state.json` (slice 4 + load-route). **Next:** slice 4 — `core/project_state.py` (M2-proper richer container) + `api/projects.py` (`POST /api/projects/load`). — **Iter 53** landed slice 2 (`29c471b`): `core/active_project.py` ships `ActiveProject` (frozen dataclass: `path`, `label`, `opened_at`), `ActiveProjectCarrier` (mutable holder with `set_active_project`/`clear`/`snapshot`/`generation`; `threading.Lock` for safe sync+async access; `validate_project_dir` runs OUTSIDE the lock for failure-atomicity), `InvalidProjectDirError(ValueError)`. New `api/dependencies.get_active_project_carrier` + `get_active_project` providers per spec §6 patterns. `bootstrap.build_app` step-9 instantiates `app.state.active_project_carrier`. Filename deliberately `core/active_project.py` (not `core/project_state.py`) so the M2-proper richer ProjectState (loaded `Project` + per-page state + GT map) lands at the spec-named module without collision. **Slice 2 deliberately does NOT**: lifespan-wire (slice 3), expose `POST /api/projects/load` (slice 4), or hold a `Project` (M2 proper). **Iter 53 also closed B-51** (`ef5908d`) per D-040: `Settings.debug_unhandled_traceback: bool = True` (env `PDLABELER_DEBUG_UNHANDLED_TRACEBACK`); `error_handler.py` Exception catch-all reads it and emits `details=None` when False, while `logger.exception` still fires server-side under both flag values. **Iter 52** landed slice 1 of the startup-discovery wiring per `specs/02-backend.md §13`: new pure module `core/startup_discovery.py` exporting `validate_project_dir(path) -> bool` (is_dir + os.access(R_OK), euid-aware), `ResolvedInitialProject(path, source: Literal["cli","session_restore"])` (frozen dataclass — same hard-frozen contract as `AppState`), and `resolve_initial_project(settings, *, session_state) -> ResolvedInitialProject \| None` (CLI overrides session per spec §13 step 4; invalid CLI path → WARNING + fall-through to session-restore, NOT a refusal-to-boot — legacy parity with `cli.py:18-23`; stale session path → DEBUG; both unset/missing → None). Structured logging via `extra={"initial_project_source", "initial_project_path"}` so observability is grep-stable. 12 new tests (321→333) covering validator's four cases (happy/missing/regular-file/unreadable-skipped-as-root), CLI-overrides-session, invalid-CLI-falls-through-with-WARNING, session-restore happy path, stale-session→None (B-60 two-stage seam), no-inputs→None, SessionState.last_project_path=None defensive case, ResolvedInitialProject frozen pin. **Slice 1 deliberately does NOT**: mutate AppState (still frozen by design — mutable ProjectState container is M2 proper), enumerate `source_projects_root` (M2's `core/project_state.py`), wire into bootstrap's lifespan (no startup hook yet — M2/M3 layer that on top). Q-A8/Q-A12/Q-A13 not relevant — pure backend, no frontend toolchain, no session-state extras-tolerance change, no new CLI flag. Pre-conditions: M0, M1. **Next:** slice 3 (lifespan hook calling `resolve_initial_project` → `set_active_project`); slice 4 (`POST /api/projects/load` route). |
| M3 OCR config modal + first-page OCR | 🟡 in progress — DTO + GET + POST(stateless) endpoints + ensure_page_model contract + pure model-selection algorithm + local-model discovery walk + HF last-modified probe + discovery-pipeline wired into `_build_snapshot` (iters 7–14) | **iter 14 (loop)** shipped slice 8c-iii-c — wired `core/hf_probe.fetch_hf_last_modified` + `core/model_discovery.discover_local_pairs`/`pairs_to_model_option_records` + `core/model_selection.pick_default_keys` into `api/ocr_config._build_snapshot`. Replaces the iter-10 hardcoded `selection_reason="stock-fallback"` with the picker's real output. New module helpers: `_resolve_local_models_root()` mirrors legacy `get_shared_models_root` (OS-aware → `<data_home>/pd-ml-models`; trainer-managed dir name pinned via `MODEL_STORE_DIRNAME = "pd-ml-models"`); `_gather_records()` always emits one HF record (with `hf_last_modified=None` when probe fails — contract pin from slice 8c-iii-b: probe never raises, so `_build_snapshot` calls it without `try/except`) plus zero-or-more local records from the discovery walk. Option lists stay stock-only — surfacing HF/local options + persistence via `OCRConfigCarrier` slips to slice 8c-iv+ (the picker's keys can diverge from exposed options without violating the wire contract because POST still only accepts `"stock"` keys today). **Test isolation pin**: integration suite's `client` fixture monkeypatches `fetch_hf_last_modified` → `None` and `_resolve_local_models_root` → empty tmpdir so the suite is hermetic regardless of `huggingface_hub` install state / network reachability — discovered the hard way when the first run hit the real hub (`huggingface.co/api/models/CT2534/pd-ocr-models 200 OK`) and reported `hf-latest` instead of the previously-pinned `stock-fallback`. Two existing tests retitled + relaxed: `test_get_ocr_config_slice8a_stock_fallback` → `test_get_ocr_config_default_reason_is_hf_unreachable_no_local` (HF record present-but-unreachable + no local pair → spec-literal `hf-unreachable-no-local`, not `stock-fallback`); same for the slice-8c-i POST counterpart. +2 new tests pin the wiring through both endpoints: `test_get_ocr_config_returns_hf_latest_when_hub_reachable` (probe returns `datetime` + no local → `hf-latest`) and `test_get_ocr_config_returns_local_only_when_hub_offline_and_pair_present` (legacy-shaped on-disk pair `<root>/all/{detection,recognition}/all-{detection,recognition}-base-1700000000.pt` + probe → `None` → `local-only-hf-unreachable`). 561 → **563 green / ruff clean**. **iter 13 (loop)** shipped slice 8c-iii-b — `core/hf_probe.py` port of legacy `fetch_hf_last_modified` (legacy `pd_ocr_labeler/operations/ocr/model_selection_operations.py` lines 169–205) plus the `HF_DEFAULT_REPO = "CT2534/pd-ocr-models"` constant. Pure network probe returning `datetime \| None` — never raises: missing `huggingface_hub` → `None` (DEBUG log); any exception from `HfApi().model_info` → `None` (INFO log with repo + revision + exception); missing/None `last_modified` → `None`; naive `datetime` → tz-aware UTC; tz-aware passes through. `revision` (default `None` → `main`) and `timeout` (default `5.0s`) kwargs forwarded to `model_info`. **Contract pin in module docstring**: probe is selection-time code that must work offline → never propagate exceptions; surfacing exceptions would convert benign offline state into a 5xx on every OCR-config snapshot. Tests use `sys.modules["huggingface_hub"] = SimpleNamespace(HfApi=...)` injection (matches legacy test patterns) plus a `builtins.__import__` patch for the missing-dep path. +11 unit tests, 550 → **561 green / ruff clean**. Wiring into `api/ocr_config._build_snapshot` to replace hardcoded `stock-fallback` slips to **slice 8c-iii-c**. **iter 12 (loop)** shipped slice 8c-iii-a — `core/model_discovery.py` port of legacy `discover_local_models` + `_stem_signature` + `latest_local_mtime` (legacy lines 117–124, 257–304, 306–327). Pure filesystem walk over `<root>/<profile>/{detection,recognition}/<stem>.pt` returning `LocalModelPair` (carries det/reco paths + max-mtime); paired translator `pairs_to_model_option_records` produces the iter-11 `ModelOptionRecord` shape consumed by `pick_default_keys`. Empty-root + missing-subdir + non-`.pt` + det-only/reco-only + case-insensitive profile sort + max-mtime-across-pair + stem-without-infix all pinned. e2e test wires discovery → records → pick_default_keys (case 2 `local-only-hf-unreachable` when local pair exists with no HF record). +20 unit tests, 530 → 550 green / ruff clean (commit `28b2192`). **iter 11 (loop)** shipped slice 8c-ii — `core/model_selection.py` port of legacy `pick_default_keys` (legacy `pd_ocr_labeler/operations/ocr/model_selection_operations.py` lines 432–483). Pure decision function over a new internal `ModelOptionRecord` dataclass (key/source/hf_last_modified/local_mtime/has_detection/has_recognition/is_preferred_profile). All six `selection_reason` literals exercised: `hf-latest`, `local-newer-than-hf`, `local-only-hf-unreachable`, `hf-only`, `hf-unreachable-no-local`, `stock-fallback`. Det/reco can diverge (det-only or reco-only local records). Module-level assertion pins the SelectionReason set against `GetOCRConfigResponse.selection_reason.__args__` so DTO drift fails at import. Discovery (HF probe + local-models walk + mtime collection) is adapter-shaped → slice 8c-iii. Wiring into `_build_snapshot` (replacing the hardcoded `stock-fallback`) is slice 8c-iii's job, NOT this slice's — the helper still echoes `stock-fallback` for now. +15 unit tests, 515 → **530 green / ruff clean**. **iter 10 (loop)** shipped slice 8c-i — `POST /api/ocr-config/models` route at the spec-canonical URL (`specs/02-backend.md §5.8` line 320). Validates `SetOCRModelsRequest` keys against the slice-8a stock-only option lists; unknown keys → 400. **Stateless echo**: returns `GetOCRConfigResponse` with `selected_*` reflecting the request, `selection_reason="stock-fallback"` (honest — no probing exists yet). Selection is NOT persisted; `OCRConfigCarrier` + `ocr_config.json` writeback is slice 8c-ii+ work. +8 integration tests (extras-forbid surfaces through the project's unified validation_error envelope as 400, not the FastAPI default 422 — pinned). 507 → **515 green / ruff clean**. **iter 9 (loop)** landed slice 8b — `core/page_state.ensure_page_model` lazy-page-load dispatcher contract (free function + `PageLoader` Protocol + `force_ocr` kwarg; cache key raw int in `state.page_states`; lock held across full load; `PageIndexOutOfRangeError(IndexError)`/`PageImageNotFoundError(FileNotFoundError)`; OCR errors propagate, not cached) + closed **B-68** (`__main__.main` daemon thread polls `socket.create_connection` until uvicorn binds, then opens browser; 10s deadline). +14+3 tests. **iter 8** shipped slice 8a — `GET /api/ocr-config` router (note: spec uses hyphenated path `/api/ocr-config` per `specs/02-backend.md` §5.8 lines 317–322; spec is source of truth). Composes iter-7 DTOs with hardcoded `selection_reason="stock-fallback"` (no model load yet). +6 tests, **490 green / ruff clean** (commit `720dcdb`). **iter 7** shipped `core/ocr_models.py` (`OCRModelOption`, `GetOCRConfigResponse`, `SetOCRModelsRequest`) per `specs/01-data-models.md` lines 377–400 — pydantic v2 frozen models, +16 unit tests (commit `9201caa`). Next: (8b) `core/page_state.ensure_page_model` test-first contract scaffold — pin idempotency, cache-key shape, lock discipline, exception types; stub the actual DocTR run (real model invocation slips to a later slice). Then `POST /api/ocr-config` router + `core/page_state` first-page run wiring. |
| M4 Image viewport + overlays + drag selection | ⬜ not started | |
| M5 Word matches view (right pane) | ⬜ not started | |
| M6 Toolbar action grid + style/component apply + add-word | ⬜ not started | |
| M7 Word edit dialog + word-image canvas + bbox edit | ⬜ not started | |
| M8 Save / Load page + Save Project + Rematch GT + driver compat | ⬜ not started | |
| M9 Export + cleanup + cut-over | ⬜ not started | |
| M9.1 Manual rotation buttons | ⬜ blocked | Pre: M9. |
| M9.2 Auto-rotation pass | ⬜ blocked | Pre: M9.1 + pd-book-tools `rotation` module. |
| M9.5 Full keyboard-driven editing audit | ⬜ blocked | Pre: M9. |
| M10 Text normalization | ⬜ blocked | Pre: pd-book-tools `text.normalize`. |
| M11 Glyph-level annotations | ⬜ blocked | Pre: M9 + pd-book-tools/pd-ocr-trainer upstreams + Q-A5/A6/A7. |

## M0 sub-tasks

- [x] **Iter 1.** Backend skeleton: `pyproject.toml`, `__init__`,
  `settings.py`, `bootstrap.py`, `__main__.py`, `api/healthz.py`,
  `api/env_js.py`, `static/.gitkeep`, `build_hooks/spa_check.py`,
  unit tests for `/healthz`, `/env.js`, settings, `build_app`.
- [x] **Iter 2.** Frontend scaffold (files only): `frontend/`
  package.json, tsconfig.{,app,node}.json, vite.config.ts,
  vitest.config.ts, index.html, src/{main,App}.tsx, App smoke test
  (`getByTestId("app-shell")`). React 19 + Vite 6 + Vitest 2;
  mirrors pgdp-prep layout. **Not yet `npm install`-ed** — the
  devcontainer lacks Node; tracked as Q-A8 in OPEN_QUESTIONS.md, to
  be verified when `mise.toml` lands.
- [x] **Iter 3.** `mise.toml` (Node 24 / Python 3.13) + Makefile
  mirroring pd-prep-for-pgdp targets (`setup`, `test`,
  `frontend-install`, `frontend-build`, `frontend-test`,
  `frontend-dev`, `openapi-export`, `build`, `ci`, plus mise
  helpers). Added `tests/unit/test_makefile.py` (parse + dry-run
  smoke). Q-A8 still open: devcontainer has no node/npm/mise; see
  iter-3 update note in `OPEN_QUESTIONS.md`.
- [x] **Iter 4.** `.pre-commit-config.yaml` mirroring
  pd-prep-for-pgdp (pre-commit-update; trailing-whitespace,
  end-of-file-fixer, check-yaml, check-json; ruff-check ×2 +
  ruff-format). Added `tests/unit/test_pre_commit_config.py` (5
  tests: YAML parse, repos shape, expected hook IDs per repo, every
  repo pins a `rev`). Drive-by reformat of
  `tests/unit/test_makefile.py` so the new check-format would pass.
- [x] **Iter 5 (2026-05-06).** Code-review checkpoint per loop
  directive. 9 findings filed in `BUGS_FOUND.md`. Highest concerns:
  B-02 (vite proxy → :8765 not :8080, blocks dev frontend loop),
  B-03 (CORS `allow_credentials=True` + wildcard origin invalid),
  B-01 (`/env.js` mounted unconditionally despite spec §2.12
  api_only gate; test cements wrong shape). Suggested iter-6 jump-
  ahead fixes: B-02, B-03, B-01, B-09 (re-tag `v0.0.0`).
- [x] **Iter 6 (2026-05-06).** Fixed B-02 (vite dev proxy
  `localhost:8765` → `localhost:8080`, three keys) and B-03 (dropped
  `allow_credentials=True` from CORSMiddleware to match pgdp-prep +
  CORS spec). Added `tests/unit/test_vite_config.py` (3 tests: file
  exists, all 3 proxy keys hit :8080, no stale 8765 literal) and
  `tests/unit/test_cors_middleware.py` (2 tests: wildcard+credentials
  combo refused, kwargs match pgdp-prep shape). Test count: 21 → 26.
- [x] **Iter 7 (2026-05-06).** Fixed B-01 (gate `/env.js` install on
  `settings.mode != "api_only"` per spec §2 step 12) + B-09 (retag
  `v0.0` → `v0.0.0` at same commit `2f01b17` so hatch-vcs version
  derivation is canonical PEP-440). Test count: 26 → 30 (added
  `tests/unit/test_env_js.py` with 4 parametrised tests across
  modes; relocated and tightened the prior `/env.js` shape assertion
  out of `test_healthz.py`; added `api_only`-omits-/env.js
  regression to `test_app_factory.py`). Wheel filename now
  `pd_ocr_labeler_spa-0.0.1.dev6+g6b6835b13.d20260506`. Local-only
  retag, no push.
- [x] **Iter 8 (2026-05-06).** Bundled fix for the three frontend
  config low bugs: B-05 (dropped dangling `lint` script — restoring
  it requires landing eslint + a real config in the same change,
  filed Q-A9), B-06 (package.json `openapi:gen` now reads
  frontend-local `openapi.json` matching Makefile + spec), B-08
  (split test type-checking into `tsconfig.test.json`; production
  `tsc -b` now excludes `*.{test,spec}.{ts,tsx}`, `__tests__/**`,
  `src/test/**`; vitest config wires `typecheck.tsconfig` →
  `./tsconfig.test.json`). Added `tests/unit/test_frontend_config.py`
  (5 tests: B-05 conditional invariant, B-06 cross-file path check,
  B-08 app-excludes / test-includes / vitest-wiring). Test count:
  30 → 35. ruff lint+format clean. Remaining BUGS_FOUND.md items:
  B-04 (low) + B-07 (nit).
- [x] **Iter 9 (2026-05-06).** Fixed B-04 (built `overrides` dict
  from CLI flags + `Settings(**overrides)` once; enabled
  `frozen=True` in `SettingsConfigDict`; added runtime + AST-level
  regression tests) and B-07 (dropped unused `settings` param from
  `_build_env`; pinned the no-arg signature). Added
  `docs/DEVELOPMENT.md` (prereqs, first-time setup, dev loop, build,
  CI mirror) with `tests/unit/test_development_doc.py` (4 tests:
  exists, every `make <foo>` reference resolves, Node/Python pins
  match `mise.toml`, Astral uv installer mentioned). Test count:
  35 → 42. ruff lint+format clean. **No remaining iter-5 findings.**
- [x] **Iter 10 (2026-05-06).** Code-review checkpoint (reviews iters
  6-9). 5 findings filed (`BUGS_FOUND.md` B-11..B-15): 0 blocker, 0
  high, 0 medium, 2 low (B-11 stale `__version__` after intermediate
  commits; B-12 DEVELOPMENT.md describes a dev loop that's only
  partly real in M0), 3 nit (B-13 AST scan misses
  AugAssign/AnnAssign; B-14 `_build_env` no-arg pin will trip M2's
  correct fix; B-15 CORS conditional test). All four iter-6-9 fixes
  correctly fixed their bugs without shifting failure modes. No
  blocker for moving forward.
- [x] **Iter 11 (2026-05-06).** Fixed B-12 (DEVELOPMENT.md split into
  "What you'll see in M0" + "What's coming in M1+"; new regression
  test pins the M0 callout naming `/healthz`, `/env.js`, and either
  `404` or `M1`), B-13 (AST walker now visits `AugAssign` /
  `AnnAssign` targets; self-test added covering all three forms),
  B-14 (test reframed: if `_build_env` has a `settings` param the
  body must reference it; M0 no-arg and M2 with-real-consumer both
  pass; only the misleading "takes-and-ignores" shape fails), and
  B-15 (CORS test asserts `allow_credentials is False`
  unconditionally — partial-regression diagnostics now point at the
  credentials bit directly). Test count: 42 → 44. ruff clean. Sole
  remaining iter-10 finding: **B-11** (stale `__version__`) —
  deferred to iter 12.
- [x] **Iter 12 (2026-05-06).** Fixed B-11 (last open iter-10
  finding). Approach: post-commit pre-commit hook calling
  `make refresh-version` plus `default_install_hook_types:
  [pre-commit, post-commit]` so the existing `pre-commit install` in
  `make setup` wires both. Three regression tests added: two in
  `tests/unit/test_pre_commit_config.py` (default-install-hook-types
  - local-repo refresh-version hook shape) and a new
  `tests/unit/test_version.py` module (runtime check that
  `__version__ == importlib.metadata.version("pd-ocr-labeler-spa")`
  - AST guard that `__init__.py` only ever assigns `__version__`
  from `version()` calls or a literal inside an
  `except PackageNotFoundError:` block). Test count: 44 → 48. ruff
  clean. **No iter-10 review findings remain.** Iter 13 should resume
  scaffolding — Tailwind v3.4 wiring is the recommended next chunk
  (small, well-bounded: tailwind.config.js, postcss.config.js,
  src/index.css with `@tailwind` directives, file existence + grep
  tests). Iter 15 is the next code-review checkpoint.
- [x] **Iter 13.** Tailwind v3.4 wiring: `frontend/tailwind.config.js`
  (ESM, `content: ["./index.html", "./src/**/*.{ts,tsx}"]`),
  `frontend/postcss.config.js` (tailwindcss + autoprefixer),
  `frontend/src/index.css` (three `@tailwind` directives + body
  font-family rule), `frontend/src/main.tsx` imports `./index.css`,
  `package.json` devDependencies pinned `tailwindcss@^3.4.0` /
  `postcss@^8.4.0` / `autoprefixer@^10.4.0`. Six new pytest shape
  pins in `tests/unit/test_tailwind_config.py`. shadcn/ui generators
  (`components.json` + the `ui/` primitives) deferred to a later sub-
  task — they need npm available to run `pnpm dlx shadcn-ui init`.
- [x] **Iter 14.** `Dockerfile` (two-stage Node → Python wheel) +
  `.dockerignore`. Three named stages (`spa` / `wheel` / `runtime`)
  pinned in tests so `docker build --target` consumers don't break
  when stages are renamed. `spa` uses `node:24-bookworm-slim` matching
  `mise.toml`; `wheel` and `runtime` use `python:3.13-slim-bookworm`.
  `wheel` stage uses Astral's `uv` from `ghcr.io/astral-sh/uv:latest`,
  static-pins the version via `ARG VERSION` + `sed` (mirroring
  `pd-prep-for-pgdp`) since the build context excludes `.git/`. The
  `COPY --from=spa` lands the SPA at `src/pd_ocr_labeler_spa/static/`
  — exactly where `build_hooks/spa_check.py` looks for `index.html`.
  Runtime `ENTRYPOINT` invokes `pd-ocr-labeler-ui --host 0.0.0.0
  --no-browser`; `EXPOSE 8080` matches Settings default.
  `.dockerignore` excludes `.git/`, `__pycache__/`, `.venv/`,
  `frontend/node_modules/`, `frontend/dist/`,
  `src/pd_ocr_labeler_spa/static/`, `tests/`, `specs/`, `docs/`. Nine
  new pytest shape pins in `tests/unit/test_dockerfile.py` covering
  existence, stage names, base-image major versions tracked from
  `mise.toml`, the spa→wheel handoff path, `uv build` invocation,
  the entrypoint name (read live from `pyproject.toml`), `EXPOSE
  8080`, and the host-bind invariant (0.0.0.0). Docker not available
  in the devcontainer so no `docker build` exec — text-grep style.
- [ ] shadcn/ui scaffold (`components.json`, generated `ui/`
  primitives). Blocked on Q-A8 (Node not present in devcontainer).
- [x] Makefile `docker-build` / `docker-run` targets wiring the new
  Dockerfile (specs/15 §6). Iter 18 (`b1ac8d5`): added
  `docker-build` / `docker-run` / `docker-shell`; `docker-build`
  depends on `frontend-build`; host port via `DOCKER_PORT`,
  container port pinned to 8080 (matches Settings + Dockerfile
  EXPOSE). 10 new tests in `tests/unit/test_makefile_docker.py`.
- [x] `install.sh` (uv tool installer). Iter 19 (`f540c62`): mirrors
  pd-prep-for-pgdp's installer pattern (auto-installs uv if missing
  → resolves latest tag via GitHub API → `uv tool install
  --reinstall <wheel>`). 75 lines + 10 shape pins in
  `tests/unit/test_install_sh.py`.
- [x] `install.ps1` (Windows uv tool installer). Iter 23: mirrors
  `install.sh` shape in PowerShell idiom (`$ErrorActionPreference =
  'Stop'`, `Test-Command` helper, uv-bootstrap via
  `https://astral.sh/uv/install.ps1`, `python -c` preflight,
  `/releases/latest` endpoint with B-27 parity, `uv tool install
  --reinstall <wheel>`). PowerShell 5.1+ compatible (the version
  shipped with Windows 10/11). No CUDA/GPU branch (matches
  `install.sh`; pd-ocr-labeler-spa has no GPU extras). 9 shape pins
  in `tests/unit/test_install_ps1.py`.
- [x] `.github/workflows/release.yml` (publish wheel + sdist on
  `v*` tag push). Iter 24: single `release` job on `ubuntu-latest`;
  `actions/checkout@v4` with `fetch-depth: 0` (hatch-vcs needs the
  full history + tag); Node 24 + Python 3.13 pinned to match
  `mise.toml`; `cd frontend && npm ci && npm run build` produces
  the SPA bundle; `uv build` produces wheel + sdist; in-workflow
  `python -m zipfile -l` check verifies the wheel contains
  `pd_ocr_labeler_spa/static/index.html` before publish (defence
  in depth alongside `build_hooks/spa_check.py`); both assets
  attached to the GitHub Release via `softprops/action-gh-release@v2`
  with `fail_on_unmatched_files: true`. PyPI publishing
  intentionally NOT wired (Q-A10) — no `PYPI_TOKEN` /
  `secrets.PYPI*` references; the in-workflow comment names OIDC
  trusted-publishing as the only acceptable future path. 12 shape
  pins in `tests/unit/test_release_workflow.py` (existence, valid
  YAML, `v*` tag trigger, `fetch-depth: 0`, `@v<N>` major-pin for
  every `uses:` ref, Node + Python pins sourced from `mise.toml`,
  `npm ci` not `npm install`, `uv build`, publish-or-upload step
  exists, wheel attached to Release, no `PYPI_TOKEN`/`TWINE_*`/
  `secrets.PYPI*` regressions).
- [ ] M0 acceptance gate: `make ci` green, `make build` produces a
  wheel that contains `pd_ocr_labeler_spa/static/index.html`,
  `pd-ocr-labeler-ui --no-browser --port 8080` answers `/healthz`,
  `make openapi-export` regenerates `frontend/src/api/types.ts`.

## Iteration index (this repo)

See `/workspaces/ocr-container/docs/LOOP_STATE.md` for the full per-
iteration log driven by the dev /loop.
