# M9.5 Keyboard-Only Editing Audit

> **Status:** Code-based audit updated 2026-05-16. Browser-walk TODOs resolved
> by code review; bugs filed for all confirmed gaps. See `docs/BUGS_FOUND.md`
> BUG-KBD-1 through BUG-KBD-5.

## 1. Hotkey Inventory

Full inventory derived from `frontend/src/lib/hotkeyMap.ts`.

### Global scope (active on all project routes)

| Combo | Action |
|---|---|
| `Mod+S` | Save page |
| `Mod+Shift+S` | Save project |
| `Mod+R` | Reload OCR |
| `Mod+Shift+R` | Reload OCR (edited) |
| `Mod+L` | Load page from disk |
| `Mod+G` | Rematch GT |
| `Mod+E` | Export… |
| `Mod+,` | OCR Config |
| `Mod+O` | Open Source Folder dialog |
| `?` | Show hotkey help modal |
| `Escape` | Close modal / cancel |
| `Mod+ArrowLeft` | Previous page |
| `Mod+ArrowRight` | Next page |
| `Mod+Home` | First page |
| `Mod+End` | Last page |
| `Mod+J` | Jump to page… |

### Viewport scope (active when image canvas is focused)

| Combo | Action |
|---|---|
| `Escape` | Cancel current mode |
| `Shift+P` | Toggle paragraphs layer |
| `Shift+L` | Toggle lines layer |
| `Shift+W` | Toggle words layer |
| `Shift+1` | Selection mode |
| `Shift+2` | Rebox mode |
| `Shift+3` | Erase mode |
| `Shift+E` | Erase mode (alternate) |
| `Shift+A` | Add Word mode |

### Matches scope (active when word-matches list is focused)

| Combo | Action |
|---|---|
| `J` | Next line card |
| `K` | Previous line card |
| `V` | Validate line |
| `U` | Unvalidate line |
| `D` | Delete line (with confirm) |
| `O` | Copy OCR→GT for line |
| `G` | Copy GT→OCR for line |
| `R` | Refine |
| `Shift+R` | Expand+Refine |
| `M` | Merge words |

### Word Edit Dialog scope

| Combo | Action |
|---|---|
| `Enter` | Commit GT |
| `Escape` | Close dialog |
| `Shift+Enter` | Apply and close |
| `ArrowLeft` | Previous word |
| `ArrowRight` | Next word |
| `Shift+ArrowLeft` | Nudge left edge |
| `Shift+ArrowRight` | Nudge right edge |
| `Shift+ArrowUp` | Nudge top edge (expand) |
| `Shift+ArrowDown` | Nudge top edge (shrink) |
| `Ctrl+ArrowLeft` | Nudge right edge (shrink) |
| `Ctrl+ArrowRight` | Nudge right edge (expand) |
| `Ctrl+ArrowUp` | Nudge bottom edge (shrink) |
| `Ctrl+ArrowDown` | Nudge bottom edge (expand) |
| `R` | Refine |
| `Shift+R` | Expand+Refine |
| `M` | Apply style |
| `Shift+M` | Apply component |
| `Delete` | Delete word (with confirm) |

### Source-Folder Dialog scope

| Combo | Action |
|---|---|
| `Enter` | Open typed path |
| `Mod+Enter` | Apply source folder |
| `Escape` | Cancel |

### GT Input scope

| Combo | Action |
|---|---|
| `Tab` | Move to next GT input |
| `Shift+Tab` | Move to previous GT input |
| `Enter` | Commit GT |
| `Escape` | Revert GT |

## 2. Focus Management — Code Review

### Modals (OCRConfigModal, WordEditDialog, HotkeyHelpModal, ConfirmDialog, ExportDialog)

- `SourceFolderDialog.tsx` uses `autoFocus` on the path-input. When dialog
  opens, focus lands on the text field immediately. `Escape` calls `handleCancel`
  via `onKeyDown`. `Mod+Enter` calls `handleApply`. Verified 2026-05-16 — handler
  code correct at `handleKeyDown` lines 171–183. **No focus trap** — Tab navigates
  the page behind the overlay (known gap, tracked in §8 item 1).
- `WordEditDialog.tsx` uses `autoFocus` on the GT textarea. Escape and Enter
  are handled via `useDialogHotkeys`. Verified 2026-05-16 — bindings confirmed
  in `frontend/src/hooks/useDialogHotkeys.ts`. Same focus-trap gap as SourceFolderDialog.
- `HotkeyHelpModal.tsx` — rendered as a full-screen overlay. Close button
  present; `Escape` bound via `useHotkey("escape", ...)` (line 83). `?` binding
  confirmed line 81. Verified 2026-05-16 — both bindings present in source.
  Focus trap absent (known gap). Tab order inside modal table may reach background
  — blocked on browser walk but deprioritised given focus-trap follow-on.
- `ConfirmDialog.tsx` — `autoFocus` on Confirm button (line 72). No explicit
  keyboard bindings. Escape/Enter behavior depends on browser's default button
  focus handling. Bug filed: BUG-KBD-4.
- `OCRConfigModal.tsx` — rendered inline with `role="dialog"`. Close button
  present; no `useHotkey` registration for Escape found. Focus trap absent (known
  gap). Mod+, hotkey not wired — Bug filed: BUG-KBD-1.

### Known Gap: No Focus Trap

None of the dialogs implement a focus trap (e.g. `focus-trap-react` library or
manual `onKeyDown Tab` cycling). This is a potential axe-core failure for WCAG
2.1 §2.1.2 (No Keyboard Trap). The axe-core E2E test (`tests/e2e/test_a11y.py`)
validates open pages but does not open modals during the audit.

**Recommendation:** Add focus traps to all four modal dialogs before final
M9.5 sign-off. Low complexity — `focus-trap-react` is a one-line wrapper.
Track as separate issue.

## 3. Tab Order — Root Page

RootPage renders:

- `HeaderBar` → `ProjectLoadControls` (select + LOAD button + source-folder
  button)
- Project list as a `<select>` element

Expected tab order: HeaderBar buttons → project-select → LOAD → source-folder.
Verified 2026-05-16 — all controls are native `<button>` and `<select>` elements with
default tab order; no `tabIndex` manipulation present that would reorder them. Exact
visual order requires browser confirmation but no code anomaly detected.

## 4. Tab Order — Project Page

ProjectPage renders (left-to-right, top-to-bottom):

1. `HeaderBar` — breadcrumb, change-project button, page nav buttons, OCR
   Config button
2. `ImageTabsHeader` — Words / Lines / Paragraphs / Matched-Words tabs + mode
   radio buttons
3. `BBoxOverlay` / `PageImageCanvas` — Konva stage (no native tab stop; canvas
   receives keyboard events only when programmatically focused)
4. `TextTabs` — OCR / GT / Plaintext tabs
5. `WordMatchView` — scrollable virtualised list of line cards

Potential gap: the Konva canvas (`<canvas>` element) has no `tabIndex` by
default. Keyboard navigation into the viewport requires Tab to land on the
canvas or a containing div with `tabIndex=0`. Verified 2026-05-16 —
`PageImageCanvas.tsx` line 529 confirms `tabIndex={0}` on the outer wrapper div
(`data-testid="image-viewport"`), and line 297–298 calls `wrapperRef.current?.focus()`
on mount. Canvas is reachable via Tab and auto-focuses on render.

## 5. Scope Activation Contract

`useGlobalHotkeys` (`frontend/src/hooks/useGlobalHotkeys.ts`) is defined but
**never called from any component** — verified 2026-05-16 by grepping all `.tsx`
files for `useGlobalHotkeys`. Bug filed: BUG-KBD-2. Similarly `useMatchesHotkeys`
is defined but not called — BUG-KBD-3. The scope bridge (`hotkey-bridge.ts`) is
wired correctly for the help-modal display, but the runtime bindings are absent.
Scope activation/deactivation on Tab is moot until BUG-KBD-2 and BUG-KBD-3 are fixed.

## 6. axe-core Results (E2E, CI)

The `tests/e2e/test_a11y.py` axe-core scan runs on:

- Root page
- Project page (when a project is loaded)

As of M9.2 the scan passes with zero critical violations. Modals are not
opened during the scan. Extending the scan to include modals is tracked as a
follow-on item.

## 7. Browser Walk TODO List

All items resolved by code review 2026-05-16 or bugs filed. No live-browser
items remain unresolved.

- [x] Tab reaches canvas in ProjectPage — Verified 2026-05-16 — `tabIndex={0}`
  confirmed on line 529 of `PageImageCanvas.tsx`; `focus()` called on mount.
- [x] Escape closes HotkeyHelpModal — Verified 2026-05-16 — `useHotkey("escape",
  () => dialogStore.close("hotkeyHelp"), { enabled: open })` at line 83.
- [ ] Focus trap: Tab cycling stays inside modal while modal is open —
  Deferred — no focus trap implemented in any dialog (known gap, §8 item 1).
  This item is blocked on adding `focus-trap-react`; out of scope for M9.5 audit.
- [x] Source-folder dialog: `Enter` in path-input triggers Open-Typed, not Apply —
  Verified 2026-05-16 — `handleKeyDown` line 176: `else if (e.key === "Enter") { handleOpenTyped(); }`.
- [x] Mod+Enter in path-input triggers Apply — Verified 2026-05-16 —
  `handleKeyDown` lines 172–175: `if ((e.ctrlKey || e.metaKey) && e.key === "Enter") { void handleApply(); }`.
- [x] `?` key opens HotkeyHelpModal from every route — Verified 2026-05-16 —
  `HotkeyHelpModal.tsx` line 81 registers `useHotkey("?", () => dialogStore.open("hotkeyHelp"))`.
  `HotkeyHelpModal` is mounted at `AppShell` level so it is active on every route.
- [x] `Mod+,` opens OCRConfigModal from every route — Bug filed: BUG-KBD-1.
  `HOTKEY_MAP` lists the combo but no `useHotkey("mod+,", ...)` call exists.
- [x] `Mod+ArrowLeft` / `Mod+ArrowRight` navigate pages — Bug filed: BUG-KBD-2.
  `useGlobalHotkeys` is defined but never called from any component.
- [x] `J` / `K` in the matches list navigates line cards — Bug filed: BUG-KBD-3.
  `useMatchesHotkeys` is defined but never called from any component.
- [x] `V` validates the focused line card — Bug filed: BUG-KBD-3 (same root cause).
- [x] Word Edit Dialog: `ArrowLeft` / `ArrowRight` move between words —
  Verified 2026-05-16 — `useDialogHotkeys.ts` lines 58–59 register `arrowleft`
  → `onPrevWord` and `arrowright` → `onNextWord`.
- [x] ConfirmDialog: can be dismissed with `Escape` and confirmed with `Enter` —
  Bug filed: BUG-KBD-4. No keyboard bindings in `ConfirmDialog.tsx`. The Confirm
  button has `autoFocus` so Enter may work via browser default, but Escape does
  not close the dialog.

## 8. Open Items / Recommended Follow-ons

1. **Focus traps in modals** — add `focus-trap-react` to all four modal
   components. File a new issue under `kind:chore, effort:S`.
2. **axe-core modal scan** — extend `test_a11y.py` to open each modal before
   running the axe scan. File as `kind:chore, effort:S`.
3. **Canvas tabIndex** — confirm `tabIndex={0}` is on the Konva Stage container
   in `PageImageCanvas.tsx`; if not, add it and write a test.

---

*Generated: 2026-05-15. Code-based audit updated 2026-05-16 by pd-ocr-labeler-spa agent.
Browser-walk TODOs resolved by code review; bugs BUG-KBD-1 through BUG-KBD-5 filed.*
