---
kind: spec
status: active
owner: maintainers
created: 2026-05-06
last_verified: 2026-07-13
---

# 16 — Milestones

> **Status**: Active
> **Last updated**: 2026-05-11
> **Spec-Issue**: pdomain/pdomain-ocr-labeler-spa#36

The roadmap. Each milestone is bounded enough that a single AI coding
agent can deliver it in one session, with the listed acceptance tests
passing.

> **Required reading before starting any milestone:**
> [`00-overview.md`](../docs/architecture/00-overview.md), [`OPEN_QUESTIONS.md`](../OPEN_QUESTIONS.md),
> the per-area specs listed in the milestone.

---

## M0 — Repo scaffold

**Outcome.** A repo that lints, type-checks, builds an empty wheel,
boots `pd-ocr-labeler-ui` and serves a static "Hello" SPA. No domain
logic.

**Files to create.**

```
pdomain-ocr-labeler-spa/
├── pyproject.toml
├── uv.lock                     # generated by `uv lock`
├── Makefile
├── mise.toml
├── .pre-commit-config.yaml
├── .gitignore
├── README.md (already exists)
├── DEVELOPMENT.md
├── Dockerfile
├── install.sh / install.ps1
├── build_hooks/spa_check.py
├── .github/workflows/release.yml
├── src/pdomain_ocr_labeler_spa/
│   ├── __init__.py
│   ├── __main__.py             # uvicorn entry
│   ├── bootstrap.py            # build_app stub (returns FastAPI())
│   ├── settings.py             # Pydantic-settings stub
│   ├── api/healthz.py          # GET /healthz
│   ├── api/env_js.py
│   └── static/.gitkeep
├── frontend/
│   ├── package.json
│   ├── package-lock.json
│   ├── vite.config.ts
│   ├── vitest.config.ts
│   ├── tsconfig.{json,app.json,node.json}
│   ├── tailwind.config.ts
│   ├── postcss.config.js
│   ├── eslint.config.ts
│   ├── components.json         # shadcn config
│   ├── index.html
│   └── src/
│       ├── main.tsx
│       ├── App.tsx
│       ├── index.css
│       ├── routes.ts
│       ├── api/client.ts
│       ├── api/types.ts        # placeholder (regen by openapi-typescript)
│       └── test/setup.ts
└── tests/
    ├── conftest.py             # `client` fixture: TestClient(build_app(settings))
    └── unit/
        ├── test_healthz.py
        └── test_settings.py
```

**Specs that govern this milestone.**

- [`00-overview.md`](../docs/architecture/00-overview.md) — tech stack, repo layout
- [`02-backend.md`](../docs/architecture/02-backend.md) §1, §2, §3 — module layout, factory, settings
- [`03-frontend.md`](../docs/architecture/03-frontend.md) §1, §2 — folder layout, tech choices
- [`15-deployment-dev.md`](../docs/architecture/15-deployment-dev.md) — Makefile, CI, install

**Acceptance tests.**

- `make setup && make test` — green.
- `make frontend-test` — green (one smoke test).
- `make frontend-build` — produces `dist/` and copies to `static/`.
- `make build` — produces a wheel; `python -m zipfile -l <whl>` shows
  `pdomain_ocr_labeler_spa/static/index.html`.
- `pd-ocr-labeler-ui --no-browser --port 8080 --host 127.0.0.1` —
  responds 200 to `/healthz`, serves `index.html` at `/`.
- `make openapi-export` — produces `frontend/openapi.json` and
  `frontend/src/api/types.ts` (the latter is just empty interfaces).
- ESLint and ruff pass clean.
- Pre-commit hooks installed and pass.
- `Makefile` implements the dev-local-aware `upgrade-deps` /
  `upgrade-deps-local` pair per
  [`15-deployment-dev.md`](../docs/architecture/15-deployment-dev.md) §15: `upgrade-deps`
  refuses-with-message in a dev-local venv (editable `pdomain-book-tools`
  detected, fallback `.venv/.pd-dev-local`, or `PD_DEV_LOCAL=1`),
  while `upgrade-deps-local` performs lock + sync + dev-local
  restore + marker write. Canonical-mode behavior unchanged.

**Pre-conditions.** None.

**Rollback.** Anything outside the listed files.

---

## M1 — Settings + adapters + AppState seam

**Outcome.** `build_app(settings)` wires:

- `Settings` (env-driven, OS-aware roots).
- `IStorage(filesystem)` adapter (verbatim port from pgdp-prep, with
  path-traversal guard).
- `IAuth(none)` adapter.
- `IOCREngine(local_doctr)` Protocol — body left as `raise NotImplementedError`.
- `AppState` skeleton (no project loaded yet).
- `RequestIdMiddleware` + structured logging.
- Error handler middleware.

No real domain endpoints yet. Frontend renders an empty header bar +
"No project loaded" placeholder.

**Files to create / modify.**

- `src/pdomain_ocr_labeler_spa/adapters/storage/{base,filesystem,__init__}.py`
- `src/pdomain_ocr_labeler_spa/adapters/auth/{base,none_,__init__}.py`
- `src/pdomain_ocr_labeler_spa/adapters/ocr/{base,local_doctr,__init__}.py`
- `src/pdomain_ocr_labeler_spa/api/middleware/{error_handler,request_id,__init__}.py`
- `src/pdomain_ocr_labeler_spa/api/dependencies.py`
- `src/pdomain_ocr_labeler_spa/core/{app_state,logging_config,__init__}.py`
- `src/pdomain_ocr_labeler_spa/core/persistence/{paths,session_state,__init__}.py`
- `src/pdomain_ocr_labeler_spa/bootstrap.py` — full factory wiring
- `src/pdomain_ocr_labeler_spa/__main__.py` — CLI flags
- `frontend/src/components/HeaderBar.tsx`
- `frontend/src/components/EmptyProjectState.tsx`
- `frontend/src/pages/RootPage.tsx`
- `tests/unit/{test_app_factory,test_request_id,test_logging,test_paths}.py`
- `tests/integration/test_lifespan.py`
- `frontend/src/components/HeaderBar.test.tsx`

**Specs.**

- [`01-data-models.md`](../docs/architecture/01-data-models.md) §5, §6 — paths, OpenAPI
- [`02-backend.md`](../docs/architecture/02-backend.md) §3, §6, §7, §8, §9 — settings,
  deps, adapters, errors, logging
- [`03-frontend.md`](../docs/architecture/03-frontend.md) §4, §5 — bootstrap, app shell
- [`15-deployment-dev.md`](../docs/architecture/15-deployment-dev.md) — env vars

**Acceptance tests.**

- `tests/unit/test_app_factory.py::test_build_app_returns_fastapi` —
  no exceptions wiring all adapters.
- `tests/unit/test_request_id.py::test_request_id_echoed` — sends a
  request with `X-Request-ID: abc`, response echoes the same value;
  log line tagged with `rid=abc`.
- `tests/integration/test_lifespan.py::test_startup_shutdown_clean` —
  `TestClient(build_app(settings))` enters and exits without resource
  warnings.
- Frontend: `RootPage.test.tsx::renders_empty_state` shows the
  "No project loaded" copy.
- Driver-contract sanity: `data-testid="project-load-button"` exists on
  the header even though it's disabled.

**Pre-conditions.** M0.

---

## M2 — Project discovery + load

**Outcome.** A user can pick a project from the dropdown, click LOAD,
and see the first page's metadata (page name + source badge). No image,
no overlays, no word matches yet.

**Backend.**

- `core/project_state.py` — `ProjectState` minus per-page work.
- `core/persistence/{project_envelope,ground_truth,__init__}.py` — read
  `pages.json` / `pages_manifest.json`, scan project dirs, build `Project`.
- `api/projects.py` — `GET /api/projects`, `POST /api/projects/discover`,
  `POST /api/projects/load`, `GET /api/projects/{id}`,
  `POST /api/projects/source-root`.
- `api/pages.py` — `GET /api/projects/{id}/pages/{idx}` returning a
  partial `PagePayload` (record only, no overlays/matches).
- Session state read on startup; written on every successful load.

**Frontend.**

- `components/ProjectLoadControls.tsx` — dropdown + LOAD + folder +
  tune buttons (tune is wired to a placeholder `OCRConfigModal` in M3).
- `components/SourceFolderDialog.tsx` — full path-picker (Home / Up /
  Open Typed / Use Current / Cancel / Apply / Enter on input).
- `pages/ProjectPage.tsx` — uses `useProject` + `usePage` to fetch
  initial data; shows `PageActions` row with disabled buttons + the
  page name + source badge. No content area yet.
- `components/PageSourceBadge.tsx`
- URL handling: `/project/{id}` redirects to `/project/{id}/page/1`;
  page change updates URL.

**Specs.**

- [`08-page-actions.md`](../docs/architecture/08-page-actions.md)
- [`13-driver-contract.md`](../docs/architecture/13-driver-contract.md) §URL, §load controls
- [`12-hotkeys-a11y.md`](../docs/architecture/12-hotkeys-a11y.md) §source-folder dialog

**Acceptance tests.**

- Backend: `tests/integration/test_project_load.py::test_load_real_fixture`
  loads a fixture project, asserts `Project.total_pages == 5`,
  `current_page_index == 0`, GT map populated, session_state.json written.
- Backend: GT-variant matching tests carried over from legacy
  `test_ground_truth.py`.
- Frontend: Playwright `test_project_loading.py` ports legacy
  `tests/browser/test_project_loading.py` — pick from dropdown, click
  LOAD, URL updates, page name appears.
- Frontend: `SourceFolderDialog.test.tsx` covers Home / Up / Open Typed /
  Use Current / Apply / Cancel / Enter on input.
- Driver-contract: every `data-testid` from
  [`13-driver-contract.md`](../docs/architecture/13-driver-contract.md) §load controls
  exists.

**Pre-conditions.** M0, M1.

---

## M3 — OCR config modal + first-page OCR

**Outcome.** Project load now eagerly OCRs the first page (or pulls
from cache). The OCR config modal lets the user pick HF/local/stock
detection+recognition models, with the auto-pick reason surfaced in
notifications.

**Backend.**

- `adapters/ocr/local_doctr.py` — actually wraps
  `Document.from_image_ocr_via_doctr`; cached predictor.
- `core/ocr/{model_selection,predictor,provenance,__init__}.py` —
  ports of legacy `model_selection_operations`, `_get_or_create_predictor`,
  `_build_live_ocr_provenance`.
- `core/page_state.py` — minimal version: `ensure_page_model` (load
  cache > load saved > run OCR), `_resolve_save_directory`,
  `persist_page_to_file`. Auto-cache write after first OCR.
- `core/persistence/user_page_envelope.py` — full schema 2.1 reader
  - writer.
- `api/ocr_config.py` — `GET /api/ocr-config`,
  `POST /api/ocr-config/models`, `POST /api/ocr-config/rescan`.
- `api/pages.py::get_page` returns full `PagePayload` minus line
  matches (still a dict with `page_dict` populated).
- `api/jobs.py` — basic Job model + GET/cancel; `reload-ocr`
  endpoint queues a job.
- `core/jobs/{events,runner,handlers/reload_ocr,__init__}.py` —
  in-memory job runner + SSE.

**Frontend.**

- `components/OCRConfigModal.tsx` — detection / recognition selects,
  HF revision input, Rescan / Cancel / Apply.
- `components/PageImageCanvas.tsx` — Konva `<Stage>` + `<Image>` only.
  Shows the page's `original` cached image at scale. No overlays yet.
- `hooks/useJobProgress.ts` — full SSE port.
- "Reload OCR" button in `PageActions` calls the job endpoint and
  shows a busy toast with progress.

**Specs.**

- [`08-page-actions.md`](../docs/architecture/08-page-actions.md) (Reload OCR section)
- [`09-persistence.md`](../docs/architecture/09-persistence.md) — envelope, image cache
- [`11-notifications.md`](../docs/architecture/11-notifications.md) — busy overlay, jobs

**Acceptance tests.**

- Backend: `tests/integration/test_first_ocr.py` — load fixture,
  assert page parsed, `page_source == "ocr"`, cached envelope written
  to `<cache>/page-images/<project>_001_envelope.json`.
- Backend: `tests/integration/test_envelope_round_trip.py` —
  envelope-write → envelope-read produces same structure (golden file).
- Frontend: Playwright `test_first_ocr.py` — load project, see page
  image rendered in Konva canvas.
- Frontend: `OCRConfigModal.test.tsx` — open via tune button, close via
  Cancel/Apply, model lists populated.

**Pre-conditions.** M2.

---

## M4 — Image viewport + overlays + drag selection

**Outcome.** Page image renders with paragraph/line/word overlays.
User can toggle layer visibility, change selection mode, drag-select
on the image, and modifiers (Shift = remove, Ctrl = symmetric diff)
work as in the legacy.

**Backend.**

- `core/selection.py` — `Selection` model + helpers.
- `api/pages.py::get_matches` — returns `LineMatch[]` for the page
  with `line_filter` query param.
- `core/line_match.py` — build `LineMatch` + `WordMatch` from a `Page`.

**Frontend.**

- `components/BBoxOverlay.tsx` — Konva layer drawing rects per layer
  with the right colors (paragraphs green, lines pink, words blue).
  Match exact RGBA values from
  `pd-ocr-labeler/.../views/projects/pages/image_tabs.py:280-285`.
- Rail (`components/shell/Rail.tsx`) — Show Paragraphs / Lines / Words
  toggle buttons (`rail-layer-*`), Selection Mode targets (`rail-target-*`).
  (`ImageTabsHeader.tsx` retired D-050/D-053; controls live in Rail + canvas overlay.)
- `components/PageImageCanvas.tsx` — drag handlers (mousedown/move/up
  on the Stage). Drag rect drawn with blue dashed `#2563eb` stroke
  exactly matching legacy.
- `lib/marquee.ts` — port from pgdp-prep
  `frontend/src/lib/marquee.ts:27-117`.
- Selection store integration: drag emits selection update through
  `useSelectionStore` (optimistic) → POST to backend selection endpoint.

**Specs.**

- [`04-image-viewport.md`](../docs/architecture/04-image-viewport.md) — full
- [`12-hotkeys-a11y.md`](../docs/architecture/12-hotkeys-a11y.md) — modifier keys

**Acceptance tests.**

- Backend: `tests/unit/test_line_match.py` — given a saved fixture
  envelope, build LineMatch correctly (counts, status, etc.).
- Frontend: Playwright `test_image_viewport.py` — load project,
  paragraphs/lines/words checkboxes toggle visibility, drag a box,
  selection persists across mode change.
- Driver-contract: every `data-testid` from
  [`13-driver-contract.md`](../docs/architecture/13-driver-contract.md) §image-tabs exists.

**Pre-conditions.** M3. **Renderer ADR required** — see Q-A14 in
[`OPEN_QUESTIONS.md`](../OPEN_QUESTIONS.md) and the blockquote below.

> **Pre-condition:** Renderer ADR filed (spike: compare Konva vs raw canvas on a
> 4K-page bbox overlay; outcome committed to `specs/17-decisions.md` before
> writing any M4 component code).

---

## M5 — Word matches view (right pane)

**Outcome.** Right pane renders virtualised line cards with per-word
columns. Filter toggle works. Inline GT edit (Enter / blur / Tab/Shift-Tab)
works. Validation per-word and per-line works. No toolbar yet.

**Backend.**

- `api/words.py` — `ground-truth`, `validate`, `validate-batch`.
- `api/lines.py` — `validate`, `copy-gt`.
- `core/page_state.py` — `update_word_ground_truth`,
  `toggle_word_validated`, `set_words_validated`, `copy_*` line methods.
- Auto-save to cache after every mutation (best-effort).

**Frontend.**

- `components/WordMatchView.tsx` — virtualised list of `LineCard`s,
  filter toggle.
- `components/LineCard.tsx` — header row + word table.
- `components/WordCell.tsx` — selection / image / OCR / GT input /
  status cells. Match exact RGBA tag chip colors.
- `components/WordImageSlice.tsx` — CSS-slice over the `original`
  cached image at the word's bbox.
- Hotkey: Enter commits GT, Tab/Shift-Tab moves to next/prev GT input.

**Specs.**

- [`05-word-matches.md`](../docs/architecture/05-word-matches.md) — full
- [`12-hotkeys-a11y.md`](../docs/architecture/12-hotkeys-a11y.md) — Enter/Tab on GT inputs
- [`13-driver-contract.md`](../docs/architecture/13-driver-contract.md) §word cell testids

**Acceptance tests.**

- Backend: `tests/integration/test_inline_gt_edit.py` — POST inline
  edit, GET matches, see updated text + status (auto re-classify).
- Frontend: Playwright `test_word_match.py` ports legacy
  `tests/browser/test_word_match.py`.
- Frontend: Playwright `test_word_match_inline_edit.py` — Tab/Shift-Tab
  navigates GT inputs in reading order; Enter commits.
- Backend: `test_word_validation.py` (port legacy) — toggle, batch.

**Pre-conditions.** M4.

---

## M6 — Toolbar action grid + style/component apply + add-word

**Outcome.** Full Page/Paragraph/Line/Word scope toolbar wired. Apply
Style / Apply Component / Clear Component rows wired. Add Word drag
mode wired.

**Backend.**

- `api/words.py` — `style`, `component`, `add`, `delete-batch`,
  `style-batch`, `component-batch`.
- `api/lines.py` — `delete-batch`, `merge`, `split-after-word`,
  `split-with-selected-words`.
- `api/paragraphs.py` — `merge`, `split-after-line`,
  `group-selected-words`, `delete-batch`.
- `api/refine.py` — `refine` (selection scopes — page-scope is M7).
- `core/operations/{bbox,line,word}_operations.py` — selection-scoped
  helpers.

**Frontend.**

- `components/ToolbarActionGrid.tsx` — 14-column grid with one row per
  scope. Buttons disabled per `update_button_state` semantics.
- `components/ApplyStyleRow.tsx` — Style select / Scope select / Apply
  button + Component select / Apply / Clear.
- `components/AddWordRow.tsx` — Add Word button toggling viewport
  mode.
- Mutation hooks for all batch endpoints.

**Specs.**

- [`06-toolbar-actions.md`](../docs/architecture/06-toolbar-actions.md) — full
- [`13-driver-contract.md`](../docs/architecture/13-driver-contract.md) §toolbar testids

**Acceptance tests.**

- Backend: `tests/integration/test_toolbar_actions.py` — every batch
  endpoint round-trips.
- Frontend: Playwright `test_toolbar_*_actions.py` (port from legacy
  `test_toolbar_{page,paragraph,line,word}_actions.py`).
- Driver-contract: every `data-testid` listed for the toolbar exists.

**Pre-conditions.** M5.

---

## M7 — Word edit dialog + word-image canvas + bbox edit

**Outcome.** Per-word edit dialog opens, with three-preview-column
layout, interactive word image with split-marker placement, drag-erase
mode, nudge grid, crop buttons, refine/expand-refine, merge/split,
tag chips. Page-scope `Refine all bboxes` button works.

**Backend.**

- `api/refine.py` — page-scope refine queues a job.
- `api/words.py` — `rebox`, `nudge`, `split`, `merge`, `erase-pixels`.
- `core/jobs/handlers/refine_bboxes.py` — page + project handlers.

**Frontend.**

- `components/WordEditDialog.tsx` — full layout per spec.
- `components/PageImageCanvas.tsx` — extend to support rebox / add /
  erase modes (already partially done in M4 for selection).

**Specs.**

- [`26-right-panel-detail.md`](../docs/architecture/26-right-panel-detail.md) — current
- [`12-hotkeys-a11y.md`](../docs/architecture/12-hotkeys-a11y.md) — Enter on dialog GT,
  Escape close

**Acceptance tests.**

- Backend: `tests/integration/test_refine_bboxes.py` — page-scope
  refine produces a job, SSE emits progress, terminal `complete`
  event, page payload reflects new bboxes.
- Frontend: Playwright `test_word_edit_dialog.py` (port from legacy).
- Frontend: Playwright `test_page_actions.py::refine_bboxes` — refine
  button triggers job, busy overlay shown, dismisses on completion.

**Pre-conditions.** M6.

---

## M8 — Save / Load page + Save Project + Rematch GT + driver compat

**Outcome.** Explicit Save / Load / Rematch GT buttons work. Save
Project iterates all loaded pages. Page source badge flips correctly.
The `pd-ocr-labeler-driver` agent operates the SPA UI without code
changes.

**Backend.**

- `api/pages.py` — `save`, `load`, `rematch-gt` finalised.
- `api/projects.py::save-all` — wraps in a job (`SAVE_PROJECT`).
- `core/page_state.py` — auto-save vs explicit save semantics
  (`update_page_source` flag).

**Frontend.**

- `components/PageActions.tsx` — wire Save Page, Save Project, Load
  Page, Rematch GT to mutations + SSE jobs as appropriate.
- `components/SaveStatus.tsx` — small "saved 2s ago" indicator near
  the page name, fed by the most recent save mutation timestamp.
- All `data-testid` and URL invariants from the driver contract are
  asserted in a single test.

**Specs.**

- [`08-page-actions.md`](../docs/architecture/08-page-actions.md) — full
- [`13-driver-contract.md`](../docs/architecture/13-driver-contract.md) — full
  conformance

**Acceptance tests.**

- Backend: `tests/integration/test_save_load_round_trip.py` — save
  page → load page → assert state matches, image fingerprint matches.
- Backend: `tests/integration/test_save_project.py` — multi-page job
  with progress events.
- Frontend: Playwright `test_page_actions.py` (port full).
- Frontend: Playwright `test_driver_contract.py` — every testid +
  URL invariant from spec 13 asserted on a freshly-loaded project.
- Driver-agent dry run: a manual test from `pd-ocr-labeler-driver`
  operates the new UI on a fixture project (run by the human, not CI).

**Pre-conditions.** M7.

---

## M9 — Export + cleanup + cut-over

**Outcome.** Export dialog works (current page or all validated, with
style filter). Notification stream is fully wired. Final QA pass.
Optional: declare GA + remove the legacy `pd-ocr-labeler` repo.

**Backend.**

- `api/export.py` + `core/jobs/handlers/export.py` — runs
  `DocTRExportOperations` in a background thread, emits per-page
  progress, surfaces results.
- `api/notifications.py` — full SSE.

**Frontend.**

- `components/ExportDialog.tsx` — scope radio, style checkboxes,
  results panel, Close.
- Final pass on `BusyOverlay` / `ProjectLoadingOverlay` sequencing.
- Final pass on hotkey help affordance (a "?" button or shortcut).

**Specs.**

- [`10-export.md`](../docs/architecture/10-export.md) — full
- [`11-notifications.md`](../docs/architecture/11-notifications.md) — full

**Acceptance tests.**

- Backend: `tests/integration/test_export.py` — current/all-validated
  scopes; per-style subfolder layout; resulting `labels.json` matches
  legacy schema byte-for-byte (golden).
- Frontend: Playwright `test_export_dialog.py`.
- E2E coverage report: every page in the legacy `tests/browser/`
  matches a Playwright test in this repo.
- Manual: a labeled project saved by the legacy is opened by the SPA
  and saved back; legacy reads the SPA's save and shows identical
  state.

**Pre-conditions.** M8.

---

## M9.1 — Manual rotation buttons

**Outcome.** Rotate ↺ / ↻ buttons in `<PageActions />`. POST
`.../rotate {degrees: ±90}` triggers Reload OCR with the rotated
image. `rotation-badge` shows current rotation.

**Files.**

- `src/pdomain_ocr_labeler_spa/api/pages.py` — new POST `.../rotate`.
- `src/pdomain_ocr_labeler_spa/core/page_state.py` — `rotate_page(degrees)` mutator.
- `src/pdomain_ocr_labeler_spa/core/jobs/handlers/rotate_page.py`.
- `frontend/src/components/PageActions.tsx` — buttons.
- `frontend/src/components/RotationBadge.tsx` — new component.
- `tests/integration/test_rotate_endpoint.py`.
- `tests/e2e/test_manual_rotate.py`.

**Specs.** [`19-auto-rotation.md`](../docs/architecture/19-auto-rotation.md) §1.1, §4.

**Acceptance tests.**

- Frontend: `data-testid="rotate-cw-button"` and `data-testid="rotate-ccw-button"`
  clickable in `<PageActions />`.
- Frontend: Playwright `test_manual_rotate.py::rotate_cw` — click
  `rotate-cw-button`, image visibly rotates, matches view re-renders
  with rotated bboxes, `rotation-badge` shows ±90°.
- Backend: `tests/integration/test_rotate_endpoint.py` — POST
  `.../rotate {degrees: 90}` persists `rotation_degrees=90` in envelope.

**Pre-conditions.** M9.

---

## M9.2 — Auto-rotation pass

**Outcome.** Project-load pass detects rotation per page;
configurable method (`gt-best-match` if GT available else `layout`).
Indicator badge distinguishes auto vs manual.

**Files.**

- `src/pdomain_ocr_labeler_spa/core/page_state.py` — auto-rotate hook in
  `ensure_page_model`.
- `src/pdomain_ocr_labeler_spa/core/jobs/handlers/auto_rotate_all.py`.
- `frontend/src/components/OCRConfigModal.tsx` — new "Auto-rotation"
  section.
- `tests/integration/test_gt_best_match_rotation.py`.
- `tests/e2e/test_auto_rotate_indicator.py`.

**Acceptance tests.**

- Frontend: `<OCRConfigModal>` contains an "Auto-rotation" section
  with `data-testid="auto-rotate-checkbox"` toggle and
  `data-testid="auto-rotate-method-select"` dropdown (gt-best-match /
  layout).
- Backend: `tests/integration/test_gt_best_match_rotation.py` —
  fixture with sideways scan auto-rotates to 90° on load; envelope
  persists `rotation_degrees=90` with `rotation_source="auto_layout"`.
- Frontend: Playwright `test_auto_rotate_indicator.py` — badge shows
  "↻ 90° auto"; user can toggle auto-rotation off via modal; disable
  reverts badge.

**Pre-conditions.** M9.1, plus pdomain-book-tools `rotation` module
shipping (delegated 2026-05-06).

---

## M9.5 — Full keyboard-driven editing audit (D-022)

**Outcome.** Every action in the labeler reachable from the keyboard,
ergonomic for power users. Audit + fill in any gaps from the M5–M8
keymap; document via the `?` help modal.

**Files.**

- `frontend/src/lib/hotkeyMap.ts` — additions.
- `frontend/src/components/HotkeyHelpDialog.tsx` (M9 polish).
- `tests/e2e/test_keyboard_only_full.py` — extends `test_keyboard_only`
  to cover the full app surface.

**Specs.** [`12-hotkeys-a11y.md`](../docs/architecture/12-hotkeys-a11y.md), [D-022](17-decisions.md).

**Pre-conditions.** M9.

---

## M10 — Text normalization (D-025)

**Outcome.** OCR config gains "Text normalization" section. Labeler
uses normalization-aware GT matching when toggle on, so `ſhall` vs
`shall` reports `exact` instead of `fuzzy`. Plaintext tabs can render
ASCII-normalized via `pdomain_book_tools.text.normalize`.

**Files.**

- `src/pdomain_ocr_labeler_spa/core/line_match.py` — opt-in normalize on
  matcher.
- `src/pdomain_ocr_labeler_spa/api/ocr_config.py` — new fields.
- `frontend/src/components/OCRConfigModal.tsx` — new section.

**Specs.** [`18-text-normalization.md`](../docs/architecture/18-text-normalization.md).

**Pre-conditions.** pdomain-book-tools `pdomain_book_tools.text.normalize`
available (delegated 2026-05-06; not yet implemented).

---

## M11 — Glyph-level side-channel annotations

**Outcome.** Per-word typography annotations (CT/ST ligatures, long-s
positions, swash) editable via a new Typography section in
`<WordEditDialog>` and a chip-row under each `<WordCell>`. Page-scope
bulk-mark dialog drives recipes ("CT auto-mark", etc.) — exposed to
the `pd-ocr-labeler-driver` agent via stable testids. `UserPageEnvelope`
bumps to **v2.2** (additive `payload.glyph_annotations` keyed by
word_id; tri-state preserved on round-trip).

When pd-ocr-trainer's glyph-feature classifier is wired (via the new
`IGlyphPredictor` adapter), per-word predictions render as greyed-out
chips with accept/reject. Predictions are NOT persisted; they're
recomputed at page-fetch time.

The user-facing slot the user originally asked about ("M6 or M7
range, post-editing-UX") was tempting but the data-model depends on
**pdomain-book-tools** (`pdomain_book_tools.ocr.glyph_annotations` — NOT yet
shipped) and the predictions path on **pd-ocr-trainer** (classifier
— NOT yet shipped). Slotting after M10 keeps the SPA work blocked
only on those two upstream deliveries.

**Files to create / modify.**

- `src/pdomain_ocr_labeler_spa/core/persistence/user_page_envelope.py` —
  v2.2 reader/writer; back-compat with v2.1 (None on read).
- `src/pdomain_ocr_labeler_spa/core/glyph/predictions.py` — `IGlyphPredictor`
  Protocol; `none_` and `local_pdtrainer` adapters.
- `src/pdomain_ocr_labeler_spa/core/page_state.py` — `set_glyph_annotations`,
  `accept_glyph_prediction`, `bulk_mark_glyphs` mutators.
- `src/pdomain_ocr_labeler_spa/api/words.py` —
  POST `.../glyph-annotations`, POST `.../accept-prediction`.
- `src/pdomain_ocr_labeler_spa/api/pages.py` —
  POST `.../glyph-bulk-mark` (recipes, dry-run support).
- `src/pdomain_ocr_labeler_spa/core/models.py` — extend `WordMatch` with
  `glyph_annotations` and `glyph_predictions`.
- `frontend/src/components/glyph/GlyphAnnotationPanel.tsx` (NEW).
- `frontend/src/components/glyph/GlyphChip.tsx` (NEW).
- `frontend/src/components/glyph/BulkGlyphMarkDialog.tsx` (NEW).
- `frontend/src/components/WordCell.tsx` — corner badge + chip row.
- `frontend/src/components/WordEditDialog.tsx` — Typography section.
- `frontend/src/components/ToolbarActionGrid.tsx` — Bulk-mark button.
- `frontend/src/components/PageImageCanvas.tsx` (canvas overlay) —
  `predictions-overlay-toggle`.
  (`ImageTabsHeader.tsx` deleted; overlay toggle lives in canvas overlay.)
- `tests/unit/test_glyph_annotations_envelope.py`.
- `tests/unit/test_glyph_envelope_back_compat.py`.
- `tests/unit/test_glyph_bulk_mark.py`.
- `tests/unit/test_gt_rejects_ligature_codepoints.py`.
- `tests/e2e/test_glyph_panel.py`.
- `tests/e2e/test_bulk_glyph_mark.py`.

**Specs.** [`20-glyph-annotations.md`](20-glyph-annotations.md).
Cross-refs [`01-data-models.md`](../docs/architecture/01-data-models.md) §3, §4 (envelope
bump rules), [`05-word-matches.md`](../docs/architecture/05-word-matches.md) (`<WordCell>`
host),
[`26-right-panel-detail.md`](../docs/architecture/26-right-panel-detail.md) (Typography
section),
[`13-driver-contract.md`](../docs/architecture/13-driver-contract.md) (testid catalogue
additions).

**Acceptance tests.**

- v2.2 envelope round-trip preserves the tri-state (None / empty /
  populated) for every word.
- v2.1 envelope reads with all `glyph_annotations = None`.
- Backend bulk-mark CT recipe on the fixture project: dry-run returns
  the expected `affected_word_ids`; apply mutates and persists.
- E2E: open `<WordEditDialog>` on a `ct`-containing word, add a CT
  ligature mark via the Typography section, close, reopen — chip
  persists; save page; reload page; chip still present.
- E2E: bulk-mark dialog on a page with 5 `ct` words shows preview "5
  words will be modified", apply emits 5 corner badges in green.
- Driver-contract conformance: every testid in
  [`20-glyph-annotations.md`](20-glyph-annotations.md) §7 present
  after a project loads.
- (If classifier shipped) predictions render as greyed chips with
  accept / reject; accepting writes `source="human_confirmed"`.

**Pre-conditions.**

- M9 (editing UX solid; envelope path stable).
- `pdomain-book-tools` ships `pdomain_book_tools.ocr.glyph_annotations`
  (delegation needed — see Q-A5 in [`specs/20-glyph-annotations.md`](20-glyph-annotations.md) §11).
- `pd-ocr-trainer` ships glyph-feature classifier + inference adapter
  for the predictions half (delegation needed; the manual half of
  M11 can ship without it).
- Q-A5 (legacy envelope tolerance), Q-A6 (predictions overlay color),
  Q-A7 (per-mark provenance) resolved in
  [`specs/20-glyph-annotations.md`](20-glyph-annotations.md) §11.

> **Pre-condition:** `pdomain-book-tools` `glyph_annotations` shipped + `pd-ocr-trainer`
> glyph classifier shipped + Q-A5/Q-A6/Q-A7 (in `specs/20-glyph-annotations.md`
> §11) resolved.

---

## Cross-cutting work

These don't fit a single milestone; they accumulate across all of them:

- **OpenAPI drift check.** From M2 onward, every endpoint added must
  appear in the generated `frontend/src/api/types.ts`. CI gate added
  in M2.
- **Driver-contract conformance.** A growing test asserts that every
  `data-testid` listed in [`13-driver-contract.md`](../docs/architecture/13-driver-contract.md)
  is present after a project is loaded. Updated each milestone that
  adds UI.
- **Per-component specs.** Specs 04–11 are read in their owning
  milestone. If a milestone diverges from a spec, the spec is wrong
  — fix it first.
- **Open questions.** Each milestone's owner reads
  [`OPEN_QUESTIONS.md`](../OPEN_QUESTIONS.md) and refuses to start any
  question its milestone blocks. If unblocked but unresolved, push
  back to the human; don't guess.

---

## Estimation note

These are agent-implementable milestones. M0 ≈ 1 session; M1–M2 ≈
1–2 sessions each; M3–M5 ≈ 2–3 sessions each (because they touch
both halves of the stack and have substantial domain logic to port);
M6–M9 ≈ 1–2 sessions each (most logic is already in `pdomain-book-tools`
and the legacy `core/` modules — the SPA work is mostly UI + endpoint
plumbing).

Total: ~15–20 sessions for an AI agent operating end-to-end.

## Adversarial Review

**Accepted finding:** M0–M10 and cut-over shipped; M11 remains unimplemented backlog rather than
blocked on Q-A7.

**Stage:** migration-time current-state review on 2026-07-13.

**Source:** an independent read-only reviewer compared this document with current
code, tests, architecture, and git history.

**Result:** the review accepted the finding above and used it to declare the
metadata status. Residual risks remain explicit here or in
`docs/context/intent-map.md`; deferred or blocked behavior is not claimed as
shipped.
