# Module retired in M5b (greenfield event-store adoption).
# UserPageEnvelope/lanes/image_cache deleted; tests guard deleted behavior.
import pytest

pytestmark = pytest.mark.skip(reason="module retired in M5b")
